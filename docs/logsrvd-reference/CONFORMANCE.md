# Conformance Matrix — `sudosrv` vs. C `sudo_logsrvd`

> **Reference:** sudo 1.9.18 — `36f7128256a93571ec378daa5c209d6883036d31` (2026-07-19)  
> **Subject:** `sudosrv` `main` @ `928da84`  
> **Method:** every numbered requirement in [`01`](01-architecture.md)–[`06`](06-tls-and-security.md) was checked against the Go source, then every non-`MATCH` verdict was independently challenged by a second pass instructed to refute it. Verdict vocabulary is defined in [README.md](README.md#verdict-vocabulary).

## Summary

| Subsystem | Reqs | MATCH | INTENTIONAL | PARTIAL | DIVERGENT | ABSENT | NA |
|---|--:|--:|--:|--:|--:|--:|--:|
| [Daemon Architecture & Connection Lifecycle](01-architecture.md) | 45 | 4 | 1 | 21 | 13 | 5 | 1 |
| [Wire Protocol & Message State Machine](02-protocol.md) | 53 | 17 | 1 | 22 | 8 | 2 | 3 |
| [Configuration Surface & Defaults](03-configuration.md) | 68 | 6 | 8 | 13 | 12 | 24 | 5 |
| [Local I/O Log Storage Format](04-local-storage.md) | 51 | 13 | 1 | 16 | 17 | 4 | 0 |
| [Relay Mode & Store-and-Forward](05-relay.md) | 54 | 15 | 9 | 11 | 8 | 11 | 0 |
| [TLS, Authentication & Security Posture](06-tls-and-security.md) | 41 | 8 | 2 | 14 | 5 | 11 | 1 |
| **Total** | **312** | **63** | **22** | **97** | **63** | **57** | **10** |

Severity of the 239 requirements that are not `MATCH`/`NA`:

| breaking | high | medium | low | informational |
|--:|--:|--:|--:|--:|
| 3 | 11 | 42 | 157 | 26 |

### Effect of the adversarial pass

254 findings were challenged: **166 upheld**, **73 downgraded**, **13 upgraded**, **2 refuted outright**.

Findings whose grade moved into or out of the top two severities:

| ID | Was | Now | Why the grade moved |
|---|---|---|---|
| `ARCH-024` | INTENTIONAL / medium | DIVERGENT / breaking | The code comment at handler.go:197-207 claims parity with C, but the shipped default does the opposite: config.go:136 sets IdleTimeout=10m and applyZeroValueDefaults (config.go:182-184) restores 10m… |
| `ARCH-045` | DIVERGENT / medium | DIVERGENT / breaking | This requirement exists solely to warn that 'a reimplementation that enforces an idle read deadline will disconnect long-running interactive sessions that the C server would keep open' (01-architectu… |
| `CONF-025` | PARTIAL / medium | DIVERGENT / breaking | The previous pass got the citations right and the consequence badly wrong. C side: logsrvd.c:1372-1374 adds the steady-state read event with an explicit NULL timeout and the comment 'No read timeout,… |
| `ARCH-043` | PARTIAL / medium | PARTIAL / high | The ordering claim is right and its consequence is much sharper than 'a race for the same cache directory'. Listeners bind AND listen in Start() Phase 1 (server.go:78/104), accept loops start in Phas… |
| `IOLOG-024` | DIVERGENT / medium | DIVERGENT / high | Verified on both sides and the blast radius is universal, not narrow. C writes the 'timestamp' object from evlog->event_time (the client's submit_time) into log.json at accept time, before eventlog_s… |
| `RELAY-010` | ABSENT / medium | ABSENT / high | The previous pass got the facts right and the severity wrong. Traced end to end through the reference client: with C's default (store_first=false, confirmed by calloc at logsrvd_conf.c:1630) a dead u… |
| `RELAY-034` | DIVERGENT / medium | DIVERGENT / high | Both claims verified and the severity was understated. C: journal_accept (logsrvd_journal.c:559-563) short-circuits on `closure->journal_path != NULL` by calling journal_write, i.e. the sub-command A… |
| `CONF-035` | ABSENT / high | ABSENT / medium | Directly parallel to the TLS-015 calibration, and the previous pass's own notes concede the point without applying it. I read logsrvd_conf_alloc: config->server.tls_check_peer = false (logsrvd_conf.c… |
| `IOLOG-010` | PARTIAL / high | PARTIAL / medium | Encoding and all three divergences confirmed against lib/iolog/iolog_nextid.c:102-139 (id>=maxseq or unparsable -> id=0 -> 1; SESSID_MAX=2176782336 per include/sudo_iolog.h:29) and session.go:767,811… |
| `RELAY-022` | DIVERGENT / high | DIVERGENT / medium | C claim verified (logsrvd_relay.c:869-888 sends 'relay server closed connection' downstream on EOF while state != FINISHED; logsrvd.c:297-305 unlinks only at FINISHED). But the requirement's own subj… |
| `TLS-007` | ABSENT / high | ABSENT / medium | Both claims verified: tls_init.c:294-319 loads tls_cacert or falls back to SSL_CTX_set_default_verify_paths(), fatal either way; Go's server tls.Config (server.go:100-103) sets neither ClientCAs nor… |
| `TLS-015` | ABSENT / breaking | ABSENT / medium | Citations are fine but the severity is not. logsrvd.c:1451-1462 does install SSL_VERIFY_PEER\|SSL_VERIFY_FAIL_IF_NO_PEER_CERT — but only inside `if (server_ctx != NULL && logsrvd_conf_server_tls_chec… |
| `ARCH-025` | DIVERGENT / high | DIVERGENT / low | Independently confirmed the refutation. C queues ServerHello unprompted in start_protocol (logsrvd.c:1364-1369) and handle_client_hello returns nothing (logsrvd.c:865-890); Go replies only to ClientH… |
| `ARCH-039` | DIVERGENT / high | DIVERGENT / low | The mechanism is described correctly (server.go:278 cancel, :298-302 close listeners, processor.go:91-105 deadline-now abort) but both stated consequences die. (a) 'On disk the session's log.json nev… |
| `IOLOG-020` | DIVERGENT / breaking | DIVERGENT / low | Both facts are exactly as stated: C writes sizeof(uuid_str)-1 == 36 bytes with no terminator (iolog_writer.c:728) and verify_iolog_uuid rejects anything but st_size==36 (logsrvd_local.c:495-496); Go… |
| `IOLOG-051` | DIVERGENT / breaking | DIVERGENT / low | The C citation is right (iolog_writer.c:388-407 hard-requires submituser/submithost/runuser/command; 166-171 defaults lines=24, columns=80, runuid/rungid=-1, exit_value=-1) and the Go citation is rig… |
| `TLS-016` | ABSENT / high | ABSENT / low | C claim verified (logsrvd.c:1380-1439: preverify_ok!=1 returns 0 immediately, non-leaf returns 1, leaf goes to validate_hostname). But the Go 'absence' has no independent consequence. Inbound, there… |
| `TLS-018` | PARTIAL / high | PARTIAL / low | C claim verified: hostcheck.c:256-271 falls back to matches_common_name() only when X509_get_ext_d2i returns NULL, and Go removed CN fallback in 1.15. But the consequence is double-counted and gated.… |

## Priority findings

> **The single highest-value fix is the idle-timeout default.** Three requirements in this list (`ARCH-024`, `ARCH-045`, `CONF-025`) are the same defect, reached independently from two subsystems. The chain, verified end to end in the reference sources:
>
> 1. `plugins/sudoers/defaults.c:610` — `def_ignore_iolog_errors = false` **by default**.
> 2. `plugins/sudoers/log_client.c:1919-1921` — on a log-server read failure the client calls `loopbreak()`, commented *"Break out of sudo event loop and kill the command."*
> 3. `logsrvd/logsrvd.c:1372` — the C server arms **no** read timeout: *"No read timeout, client messages may happen at arbitrary times."*
> 4. `internal/connection/handler.go:202-203` + `internal/config/config.go:136` — Go arms a **10-minute** read deadline by default.
>
> A user idle for 10 minutes at a `sudo -s` prompt therefore has the connection dropped and **their running command killed**. `idle_timeout: -1s` opts out; note that `idle_timeout: 0` silently restores 10m rather than disabling it.

14 requirements are graded `breaking` or `high` after adversarial review. These are the divergences worth acting on first.

### `ARCH-024` — The client read event has no timeout

**DIVERGENT** · severity **breaking** · spec: [ARCH-024](01-architecture.md) · Go: `internal/connection/handler.go:197-207`

with shipped defaults, an interactive `sudo -s` or `sudo vim` left idle at a prompt for 10 minutes has its connection closed by the server and the user's running command is KILLED. sudo_logsrvd never disconnects an idle client. Only idle_timeout: -1s avoids it; idle_timeout: 0 silently restores 10m.

<sub>Adversarial review (UPGRADED): The code comment at handler.go:197-207 claims parity with C, but the shipped default does the opposite: config.go:136 sets IdleTimeout=10m and applyZeroValueDefaults (config.go:182-184) restores 10m even from an explicit 0, so handler.go:202-206 arms a 10-minute read deadline before every ReadClientMessage. C arms the read event with NULL (logsrvd.c:1372-1374). The previous pass stopped at 'the sudo client sees the socket drop and reports a log-server error'. Traced through the reference client it is far worse: the client keeps its read event armed with no timeout for the whole session (log_client.c:1173-1174), so the server's close is seen immediately as nread==0 → 'lost connection to log server' (log_client.c:1876-1879) → goto bad → log_client.c:1919 loopbreak because def_ignore_iolog_errors defaults to false (defaults.c:610) → src/exec_pty.c:1450-1456 / exec_nopty.c:710-714 terminate_command().</sub>

### `ARCH-045` — Man page vs. code: `timeout` is a write deadline, not an idle deadline

**DIVERGENT** · severity **breaking** · spec: [ARCH-045](01-architecture.md) · Go: `internal/connection/handler.go:197-207, internal/protocol/processor.go:114-150`

same root cause as ARCH-024: with shipped defaults a sudo session idle for 10 minutes has the user's running command killed by the server's read deadline, while a client that stops reading is never timed out at all. Both halves of the reference's warning are realized simultaneously.

<sub>Adversarial review (UPGRADED): This requirement exists solely to warn that 'a reimplementation that enforces an idle read deadline will disconnect long-running interactive sessions that the C server would keep open' (01-architecture.md:1017-1019), and Go does exactly that by default — handler.go:202-206 arms idle_timeout (config.go:136, 10m) as a read deadline while processor.go:114-150 arms no write deadline at all, the precise inversion of C's NULL read timeout plus server.timeout write/handshake deadline (logsrvd.c:1372-1374, 498/1174/1308/1368/1643). Grading the very failure a requirement was written to prevent as 'medium' is too soft once the client chain is traced: log_client.c:1173-1174 (read event armed, no timeout) → :1876-1879 'lost connection to log server' → :1919 loopbreak with def_ignore_iolog_errors=false (defaults.c:610) → src/exec_pty.c:1450-1456 terminate_command.</sub>

### `CONF-025` — `[server] timeout`

**DIVERGENT** · severity **breaking** · spec: [CONF-025](03-configuration.md) · Go: `internal/config/config.go:50,136,176-184; internal/connection/handler.go:197-207; internal/protocol/processor.go:114-150`

With shipped defaults, a real sudo client's interactive root shell is killed after 10 (or 30) minutes of terminal inactivity, and the tail of the I/O log is lost. C never disconnects an idle client. Opt-out exists (idle_timeout: -1s, documented at config.go:176-181) but the default is the breaking one.

<sub>Adversarial review (UPGRADED): The previous pass got the citations right and the consequence badly wrong. C side: logsrvd.c:1372-1374 adds the steady-state read event with an explicit NULL timeout and the comment 'No read timeout, client messages may happen at arbitrary times', and the reference client is symmetric — log_client.c:1170-1174 'Server messages may occur at any time, so no timeout.' Neither end ever times out an idle session. Go side: handler.go:202-207 arms conn.SetReadDeadline(now + IdleTimeout) on every loop iteration, with IdleTimeout defaulting to 10m (config.go:136) and forced back to 10m if zeroed (config.go:182-184); the shipped configs set 30m. Now trace the consequence through the real client: an interactive session (sudo -s / sudo -i) left at a prompt sends no ClientMessage, the deadline fires, the handler returns and the socket closes; log_client.c:1877-1879 reads 0 bytes, warns 'lost connection to log server' and goes to bad; log_client.c:1918-1921 sees ignore_log_errors false (def_ignore_iolog_errors defaults to false, defaults.c:610) and calls read_ev->loopbreak; src/exec_pty.c:1449-1460 observes sudo_ev_got_break and calls terminate_command(ec->cmnd_pid, true), SIGKILLing the user's shell. Separately, part (a) is also confirmed: withWriteContext (processor.go:112-148) only sets a write deadline to force a fast close on ctx cancellation and never applies a time-based bound, so C's `timeout` on write events has no analogue.</sub>

### `ARCH-032` — Write event lifetime and drain-triggered close

**DIVERGENT** · severity **high** · spec: [ARCH-032](01-architecture.md) · Go: `internal/connection/handler.go:239, internal/connection/handler.go:246, internal/protocol/processor.go:114-150`

an unauthenticated peer that completes the handshake and stops reading pins a handler goroutine, its session files and one of the 10000 connection slots until process shutdown. Repeat to wedge the server. C reaps such a peer after 30 s by default.

<sub>Adversarial review (UPHELD): Both sides verified. Go: withWriteContext (processor.go:114-150) installs a deadline watcher only for ctx.Done(); in steady state writeFull (processor.go:152-164) blocks indefinitely. C: server_msg_cb closes on SUDO_EV_TIMEOUT with 'timed out writing to client %s' (logsrvd.c:1042-1045) and the write event is armed with server.timeout, defaulted to DEFAULT_SOCKET_TIMEOUT_SEC (logsrvd_conf.c:1653). I also checked reachability rather than assuming it: an attacker does not need to wait for 10-second commit points, because handler.go:299-304 replies to every unrecognised pre-session message with an error ServerMessage and keeps reading, so at the 100 msg/s rate limit a peer that never reads fills the socket send buffer in under a minute.</sub>

### `ARCH-036` — State admissibility per message type

**PARTIAL** · severity **high** · spec: [ARCH-036](01-architecture.md) · Go: `internal/connection/handler.go:255-305`

one crafted message from an unauthenticated peer makes the relay either (a) hot-loop connect/replay/fail against the upstream forever, re-delivering duplicate partial sessions on every pass, or (b) deliver a truncated session and retire the cache file. A buggy or hostile client can also hold a connection open indefinitely emitting messages C would kill on the first one.

<sub>Adversarial review (UPHELD): All four sub-claims verified against handler.go:255-305, storage/session.go:960-963 (mid-session ClientHello warns and is ignored), :519-520 (event-only session errors), and relay/session.go:507 (everything is pushed into fromClientChan). C's table at logsrvd.c:523-527/593-597/665-669/873-877 confirmed. I then chased the relay consequence to the end rather than asserting it: writeMessagesToCache (relay/session.go:325-332) writes the poisoned ClientHello into the cache verbatim, flushFile (relay/session.go:738-772) replays every cached record to the upstream, and the upstream C daemon rejects a ClientHello in RUNNING with 'state machine error' and closes. Worse, the retry loop in run() (relay/session.go:265-277) applies backoff only on connect failure — a flush failure falls straight through to the next connectToUpstream with no sleep.</sub>

### `ARCH-043` — Outgoing relay queue is scanned once at startup

**PARTIAL** · severity **high** · spec: [ARCH-043](01-architecture.md) · Go: `internal/server/server.go:158-164, internal/relay/session.go:584-660`

a relay restarting with a large recovery backlog can silently truncate and duplicate a live session's audit record upstream and lose the rest, then hot-loop retrying; and an unreadable or wrongly-mounted cache directory lets the daemon come up healthy-looking while permanently abandoning every cached session.

<sub>Adversarial review (UPGRADED): The ordering claim is right and its consequence is much sharper than 'a race for the same cache directory'. Listeners bind AND listen in Start() Phase 1 (server.go:78/104), accept loops start in Phase 2 (server.go:132-136), and RecoverOrphans is only spawned in Phase 3 (server.go:158-164). RecoverOrphans globs `*.log` over the whole cache root (relay/session.go:608) — the same flat namespace in which a live session's writer creates `{sessionUUID}.log` (relay/session.go:287). Any live cache file that exists when that glob runs is renamed to *.log.flushing by FlushOrphanedFile (relay/session.go:668-671), partially read, shipped upstream, and then unlinked by retireCacheFile (relay/session.go:706-709) while the session's writer is still appending to the now-unlinked inode; the session's own flush phase then opens `{uuid}.log`, gets ENOENT, and enters the no-backoff retry loop. The window is normally sub-millisecond, but the two Glob passes over a cache directory holding thousands of orphans after an outage take seconds — exactly the situation in which the directory is full and clients are queued in the listen backlog. The second half also survives, by a different mechanism than claimed: filepath.Glob swallows directory read errors, so an unreadable cache dir yields zero orphans and an INFO line 'No orphaned relay files found' rather than the error server.go:161 is watching for, where C exits EXIT_FAILURE (logsrvd.c:2288-2291).</sub>

### `IOLOG-024` — The `timestamp` object holds the client's submit time in seconds+nanoseconds

**DIVERGENT** · severity **high** · spec: [IOLOG-024](04-local-storage.md) · Go: `internal/storage/session.go:1022-1023,1673-1680`

sudoreplay -l reports every completed Go-written session as starting at the moment the command exited on the server (off by the whole command duration plus clock skew) and every aborted session as 'Dec 31 1969'; date-range searches with fromdate/todate silently return the wrong set. The correct submit time is in the legacy `log` file but is never consulted because log.json exists.

<sub>Adversarial review (UPGRADED): Verified on both sides and the blast radius is universal, not narrow. C writes the 'timestamp' object from evlog->event_time (the client's submit_time) into log.json at accept time, before eventlog_store_json (lib/iolog/iolog_loginfo.c:163-181). Go's initialize (session.go:1018-1023) writes only server_log_id and an RFC3339 string under the unknown key 'submit_time'; 'timestamp' appears only in finalize (session.go:1673-1680) and holds time.Now(). Confirmed the consumer side: parse_json.c:481 maps 'timestamp' -> json_store_timestamp -> evlog->event_time, iolog_parse_loginfo prefers log.json over log (iolog_loginfo.c:58-61), and sudoreplay.c:1448 prints get_timestr(evlog->event_time.tv_sec) for every -l entry while sudoreplay.c:1409-1412 uses event_time for fromdate/todate matching. Unlike the restart findings this needs no unusual client — it is wrong for 100% of sessions.</sub>

### `IOLOG-045` — Uncompressed resume seeks; it does not truncate; the resume point must match exactly

**DIVERGENT** · severity **high** · spec: [IOLOG-045](04-local-storage.md) · Go: `internal/storage/session.go:1405-1490,1567-1609`

A client that holds a log_id for an unfinalized session can reconnect, send RestartMessage{resume_point:[0,0]}, and the server truncates timing, stdin, stdout, stderr, ttyin and ttyout to zero bytes — the entire recorded transcript is destroyed while uuid/log/log.json remain, leaving a session sudoreplay replays as empty. C leaves the bytes on disk. Secondary: on a non-aligned resume point Go warns and falls back to append (session.go:1570-1574), producing a log with the overlap duplicated, where C aborts the restart.

<sub>Adversarial review (UPHELD): Both citations check out but the stated trigger is wrong and the real one is worse. C: logsrv_util.c:133-137 returns true immediately for an unset target, leaving the r+ handles at offset 0 with the file contents intact; iolog_seekto never truncates and aborts on overshoot/EOF (logsrv_util.c:149-152,177-181). Go: computeResumeOffsetsAt (session.go:1407-1409) returns (0, empty map, nil) for target<=0, so useSeek stays true and openForRestartAt (session.go:1481-1487) calls f.Truncate(0) on timing and on every existing stream. The previous pass justified this as 'the normal case when a connection drops' — that is false: plugins/sudoers/log_client.c:1198-1232 puts fmt_restart_message inside '#ifdef notyet' and log_client.h:63 marks SEND_RESTART 'TODO: currently unimplemented', so a real sudo client never restarts. The finding survives anyway on a different chain: handler.go:290 accepts a RestartMessage as a pre-session message on any connection, the only gate is the uuid embedded in the log_id (session.go:1536-1543) which the server itself handed to the client, and the timing-writability gate (1546-1552) is open for any session that has not received an ExitMessage.</sub>

### `RELAY-010` — Exhausting the relay list produces a specific error to the client

**ABSENT** · severity **high** · spec: [RELAY-010](05-relay.md) · Go: `internal/relay/session.go:239-283, internal/connection/handler.go:473-542`

With a permanently unreachable upstream, a C-default relay blocks privileged command execution (fail-closed on audit); the Go relay permits it indefinitely and spools locally, with only a server-side slog warning.

<sub>Adversarial review (UPGRADED): The previous pass got the facts right and the severity wrong. Traced end to end through the reference client: with C's default (store_first=false, confirmed by calloc at logsrvd_conf.c:1630) a dead upstream means the downstream ServerHello is never sent (logsrvd.c:1651-1659) or an `error = "unable to connect to relay host"` arrives (logsrvd_relay.c:487-491); log_client.c:2116-2127 log_server_open then returns NULL; iolog.c:762-773 sudoers_io_open_remote returns -1; and def_ignore_iolog_errors defaults to false (plugins/sudoers/defaults.c:610), so the sudo front end refuses to run the command. Go has no path that can produce that signal — the client is already gone before any upstream attempt (session.go:239-283) — so sudo runs the privileged command and reports successful remote logging. That is a security weakening relative to C's default configuration, which is the rubric's definition of high.</sub>

### `RELAY-019` — `log_id` handling is skipped when there is no downstream client

**DIVERGENT** · severity **high** · spec: [RELAY-019](05-relay.md) · Go: `internal/relay/session.go:764-771, internal/connection/handler.go:679-711`

A relay deployment with event-only sudoers (log_servers without log_input/log_output — a normal configuration) accumulates cache files that can never be retired, one immortal goroutine per session, and a fresh accept event recorded on the terminal server roughly every 5 s per stuck session, forever. The upstream's event log is flooded with duplicates of a single sudo invocation.

<sub>Adversarial review (UPHELD): Both claims verified and the chain holds. C: journal_accept (logsrvd_journal.c:559-592) and store_accept_local (logsrvd_local.c:206-220) send a log_id only inside `if (msg->expect_iobufs)`, so an accept-only session gets no reply at all. Go: handleEventOnlyAccept (handler.go:679-711) still constructs a full relay.Session, whose writeMessagesToCache writes the expect_iobufs=false AcceptMessage (session.go:311-316); the stock client then sends an ExitMessage even for non-I/O sessions (log_client.c:1476-1483 SEND_EXIT with !log_io), so the write phase completes and the flush phase starts. flushFile (session.go:764-771) then blocks on ReadServerMessageContext after *any* AcceptMsg. Additionally verified, and worse than reported: the flush-failure branch at session.go:266-278 has no sleep before `attempt++`, so the retry cadence is bounded only by the 5 s read timeout, not by calculateBackoff. Go-to-Go relaying is affected identically (handler.go:710-711).</sub>

### `RELAY-020` — Upstream `error` messages are relayed and stop relay I/O

**DIVERGENT** · severity **high** · spec: [RELAY-020](05-relay.md) · Go: `internal/relay/session.go:764-771, internal/relay/session.go:738-743, internal/relay/session.go:709-729`

An upstream that rejects the session (disk full, iolog permission failure, quota) has its error read and discarded; for any session small enough that the remaining records fit in the socket buffer before the peer's RST is observed, Go reaches EOF, deletes the cache file and logs INFO 'Cache flush successful'. A recoverable upstream rejection becomes permanent audit-trail loss.

<sub>Adversarial review (UPHELD): Verified. Go: session.go:764-771 reads exactly one ServerMessage after the AcceptMessage and discards it without inspecting the oneof; nothing is read from the upstream thereafter; on local io.EOF session.go:740-743 calls retireCacheFile, which unlinks (session.go:709-729). C: handle_server_error (logsrvd_relay.c:638-661) tears the closure down with state still RUNNING, which is neither FINISHED (logsrvd.c:297-305, no unlink) nor CONNECTING (logsrvd.c:108-111, no re-queue), so the journal survives. Reachability confirmed against the reference upstream: store_accept_local sets closure->errstr on 'error creating I/O log' / 'error logging accept event' (logsrvd_local.c:175-206), i.e. an upstream that is out of disk or has bad iolog permissions answers the very first replayed record with an error.</sub>

### `RELAY-034` — A sub-command Accept during a journalled session reuses the journal

**DIVERGENT** · severity **high** · spec: [RELAY-034](05-relay.md) · Go: `internal/relay/session.go:489-493`

With sudo intercept/log_subcmds enabled against a Go relay, every intercepted sub-command is silently dropped from the relayed audit record — the terminal server's log for that session shows only the original command, with no error anywhere.

<sub>Adversarial review (UPGRADED): Both claims verified and the severity was understated. C: journal_accept (logsrvd_journal.c:559-563) short-circuits on `closure->journal_path != NULL` by calling journal_write, i.e. the sub-command AcceptMessage IS appended and no second log_id is sent. Go: HandleClientMessage (session.go:490-493) short-circuits on *any* AcceptMsg and returns a log_id *before* the channel send, so the sub-command Accept never reaches writeMessagesToCache and therefore never reaches the upstream. Crucially, Go advertises the capability it then discards — handler.go:392-395 sets Subcommands:true in the ServerHello, which is what causes log_client.c:1008 to emit further AcceptMessages in SEND_IO state. The duplicate log_id really is harmless (log_client.c:1638-1641 keeps the first iolog_id and returns true). Silently discarding the exact records an operator enabled intercept/log_subcmds to capture is 'silently wrong logs', which is high.</sub>

### `RELAY-042` — A successful replay unlinks the journal and pumps the queue

**DIVERGENT** · severity **high** · spec: [RELAY-042](05-relay.md) · Go: `internal/relay/session.go:738-753, 709-729`

A successful write(2) into the local TCP send buffer is treated as durable delivery. If the upstream dies or fails to persist after the bytes leave Go's socket, the cache file is already unlinked and the session is gone — no error, no retry, and an INFO line claiming 'Cache flush successful'.

<sub>Adversarial review (UPHELD): Both sides verified against source. C: connection_close (logsrvd.c:296-305) unlinks only under `closure->state == FINISHED`, and FINISHED for an I/O session is only reached via the upstream's final commit_point (handle_exit sets EXITED at logsrvd.c:633-637 and leaves the promotion to schedule_commit_point when relay_closure != NULL). Go: flushFile (session.go:738-743) calls retireCacheFile — os.Remove (session.go:709-712) — the moment the *local* read hits io.EOF; it never reads the upstream after the ExitMessage and never inspects the one reply it does read (session.go:764-771). There is no comment, test or config in the repo acknowledging delete-before-ack, so INTENTIONAL is not available.</sub>

### `RELAY-054` — Store-and-forward is at-least-once; real-time relay is at-most-once

**PARTIAL** · severity **high** · spec: [RELAY-054](05-relay.md) · Go: `internal/relay/session.go:738-753, 239-283`

The net guarantee is 'at-least-once except across the final acknowledgement window, where it degrades to at-most-once': kill the upstream at the wrong moment and the session exists in neither place.

<sub>Adversarial review (UPHELD): Verified end to end. The at-most-once half is genuinely vacuous (Go has no real-time path). The at-least-once half is broken exactly as described: duplicates behave like C (an interrupted replay leaves the file and re-sends the whole session, no idempotency key), but delivery is not guaranteed because retireCacheFile fires on local io.EOF (session.go:738-743) rather than on the upstream's final commit_point, which C requires via state==FINISHED (logsrvd.c:296-305). This is the delivery-guarantee statement of RELAY-042 and it is the guarantee the entire store-and-forward design exists to provide, so it warrants its own high grade rather than being folded away.</sub>


## Full matrix

`MATCH` rows are listed for completeness — they are the evidence that the requirement was actually checked, not skipped.

### Daemon Architecture & Connection Lifecycle

Spec: [`01-architecture.md`](01-architecture.md)

| ID | Requirement | Verdict | Sev | Go source | Consequence |
|---|---|---|---|---|---|
| `ARCH-001` | Single-process, single-threaded event loop | INTENTIONAL | informational | `internal/server/server.go:237-248` | none on its own; it is the attribution anchor for ARCH-032/036/039. |
| `ARCH-002` | Command-line options | DIVERGENT | low | `cmd/sudosrv/main.go:76-100` | sudo_logsrvd-shaped invocations (-n -f conf) fail; a bad option exits 2 not 1. Operator/packaging concern only — no protocol effect. |
| `ARCH-003` | `-R` random drop applies only to IoBuffer messages | ABSENT | informational | — | none observable; a debugging aid is missing. |
| `ARCH-004` | Startup order and failure exits | DIVERGENT | low | `cmd/sudosrv/main.go:54-73, cmd/sudosrv/main.go:103-142, cmd…` | a supervisor keying on exit 1 for 'bad config' misclassifies; no client-visible effect. |
| `ARCH-005` | Process umask is 0077 | DIVERGENT | low | — | only under a non-default inherited umask (e.g. an operator adding UMask=0077) do dir_permissions/file_permissions silently tighten with no warning. Default deployment is identical to C. |
| `ARCH-006` | `RLIMIT_NOFILE` is raised at startup | ABSENT | low | — | run outside systemd the daemon inherits a 1024 soft limit and hits EMFILE far below the max_connections=10000 default (config.go:137). |
| `ARCH-007` | Daemonization | ABSENT | low | — | foreground-only; no -n equivalent needed. Operator-visible only. |
| `ARCH-008` | Pidfile creation and removal | ABSENT | low | — | operators outside systemd must locate the PID themselves. |
| `ARCH-009` | stderr diagnostics are disabled after daemonization in both… | DIVERGENT | low | `cmd/sudosrv/main.go:198` | all diagnostics land on stdout; no sudo_logsrvd.conf logfile/syslog routing equivalent. See ARCH-010 for a sharper consequence of logging to stdout. |
| `ARCH-010` | `SIGPIPE` is ignored | DIVERGENT | low | `internal/protocol/processor.go:152-164` | if the daemon's stdout is a pipe whose reader goes away (shell pipeline, container log collector, `sudosrv \| tee`), the next connection log line kills the daemon with SIGPIPE. Not reachabl… |
| `ARCH-011` | No privilege dropping, no chroot | MATCH | none | — | — |
| `ARCH-012` | One listener per resolved address; wildcard yields IPv4 and… | PARTIAL | low | `internal/server/server.go:77-110` | a hostname with several A/AAAA records is only partly bound; out of the box the server is loopback-only. Both are configuration-time, fail-closed, and immediately visible. |
| `ARCH-013` | Listener socket options and backlog | PARTIAL | low | `internal/server/server.go:78, internal/server/server.go:104` | an IPv4 peer arriving on the dual-stack socket is reported through the v4-mapped path in logs. Cosmetic. |
| `ARCH-014` | At least one listener must bind, but partial failure is tol… | DIVERGENT | medium | `internal/server/server.go:78-114` | a port conflict on the TLS port alone (rolling restart, stray process) takes the plaintext service down with it, where sudo_logsrvd would serve the address that bound. Narrow but real. |
| `ARCH-015` | Accept behavior: one connection per readable event, no cap | PARTIAL | medium | `internal/server/server.go:182-250` | the 10001st concurrent session gets an instant close; with sudoers' default ignore_iolog_errors=false (defaults.c:610) that client's command is then killed. Requires 10000 concurrent sessio… |
| `ARCH-016` | `SO_KEEPALIVE` on accepted client sockets | PARTIAL | medium | `internal/server/server.go:78` | a network partition longer than ~2.5 minutes causes the Go server to tear the connection down; when connectivity returns the sudo client kills the user's running command. C would have held… |
| `ARCH-017` | Accepted sockets remain in blocking mode | MATCH | none | `internal/protocol/processor.go:152-190` | — |
| `ARCH-018` | Peer address extraction; unsupported families rejected | PARTIAL | low | `internal/connection/handler.go:322, internal/connection/han…` | SIEM/log-scraping rules written against bare-IP fields need the ephemeral port stripped. |
| `ARCH-019` | `SIGHUP` reloads configuration and rebuilds listeners witho… | PARTIAL | medium | `internal/server/server.go:266-269, internal/server/server.g…` | (a) routine TLS certificate rotation requires a full restart, which kills every in-flight sudo command (client loopbreak → terminate_command); C rebinds with zero drops. (b) a config edit t… |
| `ARCH-020` | `SIGUSR1` dumps server state to the debug log | ABSENT | low | `internal/server/server.go:262` | strictly less visibility than sudo_logsrvd when debugging a wedged connection — which matters more given ARCH-032. |
| `ARCH-021` | Signals are handled synchronously from the event loop | MATCH | none | `internal/server/server.go:261-275` | — |
| `ARCH-022` | No `SIGCHLD` handling | MATCH | none | — | — |
| `ARCH-023` | Connection closure allocation and handler selection | PARTIAL | low | `internal/connection/handler.go:101-156` | none client-visible; allocation churn only. |
| `ARCH-024` | The client read event has no timeout | DIVERGENT | breaking | `internal/connection/handler.go:197-207` | with shipped defaults, an interactive `sudo -s` or `sudo vim` left idle at a prompt for 10 minutes has its connection closed by the server and the user's running command is KILLED. sudo_log… |
| `ARCH-025` | `start_protocol` sends `ServerHello` and arms both events | DIVERGENT | low | `internal/connection/handler.go:262-268, internal/connection…` | none for any client shipped with sudo. Only a hypothetical greet-first client or a bare TCP banner probe observes the difference. |
| `ARCH-026` | Protocol start is deferred behind TLS handshake and relay c… | PARTIAL | medium | `internal/server/server.go:104, internal/connection/handler.…` | a TLS slowloris pins a connection slot 20x longer than against sudo_logsrvd; 10000 such peers exhaust max_connections and lock out real clients. |
| `ARCH-027` | TLS handshake event flips direction and is bounded by `serv… | PARTIAL | low | `internal/server/server.go:100-108` | a failing peer learns why the handshake failed instead of seeing a bare reset. Informational-to-low. |
| `ARCH-028` | Wire framing on read: 4-byte big-endian length, 2 MiB cap | PARTIAL | low | `internal/protocol/processor.go:167-190` | a client that trips the 2 MiB cap sees an unexplained reset instead of a named error. Not reachable by the reference client, which caps its own messages. |
| `ARCH-029` | Read buffer sizing | DIVERGENT | informational | `internal/protocol/processor.go:168, internal/protocol/proce…` | none observable; an allocation-vs-retention trade. |
| `ARCH-030` | EOF handling | PARTIAL | low | `internal/connection/handler.go:209-220` | operators cannot tell a clean session end from a truncated one; diagnostic loss only. |
| `ARCH-031` | Server messages are queued, never written inline | DIVERGENT | informational | `internal/protocol/processor.go:235-256, internal/connection…` | none by itself; the real failure it enables is ARCH-032. |
| `ARCH-032` | Write event lifetime and drain-triggered close | DIVERGENT | high | `internal/connection/handler.go:239, internal/connection/han…` | an unauthenticated peer that completes the handshake and stops reading pins a handler goroutine, its session files and one of the 10000 connection slots until process shutdown. Repeat to we… |
| `ARCH-033` | TLS read/write cross-dispatch | NA | none | — | crypto/tls resolves WANT_READ/WANT_WRITE internally inside a single blocking Read or Write on the underlying non-blocking socket via the netpoller. There is no application-level event to re… |
| `ARCH-034` | Periodic commit timer: 10 s, one-shot, re-armed by I/O | PARTIAL | low | `internal/storage/session.go:38, internal/storage/session.go…` | commit-point timing on the wire differs from C; no functional effect on any shipped client. Relay-mode local commit generation is architecturally required by store-and-forward, not a defect. |
| `ARCH-035` | `ExitMessage` handling: final commit point, or immediate fi… | PARTIAL | medium | `internal/storage/session.go:952-960, internal/storage/sessi…` | a client can put arbitrary UTF-8 into the audit record's `signal` field where sudo_logsrvd would reject the message; and the socket lingers after Exit until the client closes or idle_timeou… |
| `ARCH-036` | State admissibility per message type | PARTIAL | high | `internal/connection/handler.go:255-305` | one crafted message from an unauthenticated peer makes the relay either (a) hot-loop connect/replay/fail against the upstream forever, re-delivering duplicate partial sessions on every pass… |
| `ARCH-037` | State transitions on Accept/Reject/Restart | PARTIAL | low | `internal/connection/handler.go:617-677, internal/connection…` | after a denied command the socket lingers for up to idle_timeout instead of being closed by the server. Resource-only, and only for non-stock clients. |
| `ARCH-038` | Error reporting shuts down reads and closes after the error… | PARTIAL | low | `internal/connection/handler.go:232-241` | sudo prints 'Internal Server Error' instead of one of C's sixteen specific errstr values, so client-side diagnosis requires correlating with the server's own logs. Operator-visible only, wh… |
| `ARCH-039` | Graceful shutdown on `SIGINT`/`SIGTERM` | DIVERGENT | low | `internal/server/server.go:260-322` | an in-flight client loses one final commit_point it has no use for. End-user outcome (command killed on daemon shutdown) is the same as C; on-disk outcome is the same as C. |
| `ARCH-040` | Shutdown terminates on last-connection-freed or a 10 s fail… | PARTIAL | low | `internal/server/server.go:304-321, cmd/sudosrv/main.go:28` | shutdown can take up to 30 s instead of 10 s, and only when a handler is wedged in a blocking write — i.e. only when ARCH-032 has already fired. |
| `ARCH-041` | Connection teardown order and resource release | PARTIAL | informational | `internal/connection/handler.go:160-185` | none. |
| `ARCH-042` | `connection_close` store-and-forward handoff and journal cl… | PARTIAL | low | `internal/relay/session.go:204-284, internal/connection/hand…` | none distinguishable from C; a structural difference in how retries are scheduled. |
| `ARCH-043` | Outgoing relay queue is scanned once at startup | PARTIAL | high | `internal/server/server.go:158-164, internal/relay/session.g…` | a relay restarting with a large recovery backlog can silently truncate and duplicate a live session's audit record upstream and lose the rest, then hot-loop retrying; and an unreadable or w… |
| `ARCH-044` | Clean exit path after the event loop | PARTIAL | low | `cmd/sudosrv/main.go:252-260` | process can report success while a handler was still wedged; no pidfile or config cleanup to skip. |
| `ARCH-045` | Man page vs. code: `timeout` is a write deadline, not an id… | DIVERGENT | breaking | `internal/connection/handler.go:197-207, internal/protocol/p…` | same root cause as ARCH-024: with shipped defaults a sudo session idle for 10 minutes has the user's running command killed by the server's read deadline, while a client that stops reading… |

### Wire Protocol & Message State Machine

Spec: [`02-protocol.md`](02-protocol.md)

| ID | Requirement | Verdict | Sev | Go source | Consequence |
|---|---|---|---|---|---|
| `PROTO-001` | Length-prefix framing, 32-bit big-endian | MATCH | none | `internal/protocol/processor.go:176,247` | — |
| `PROTO-002` | Maximum message size is 2 MiB, inclusive | MATCH | none | `internal/protocol/processor.go:20,177,241` | — |
| `PROTO-003` | An oversized client frame yields an `error` message, then c… | DIVERGENT | low | `internal/protocol/processor.go:177-179; internal/connection…` | Diagnostic only: a peer that oversteps 2 MiB sees a bare FIN instead of the reason string, and the server-side note is at debug level, invisible at the default info level. |
| `PROTO-004` | Multiple messages per read are drained in one pass; partial… | MATCH | none | `internal/protocol/processor.go:167-190; internal/connection…` | — |
| `PROTO-005` | Read buffer starts at 64 KiB and grows by power-of-two roun… | NA | none | `internal/protocol/processor.go:181` | Pure allocation strategy with no external contract (the spec says so itself). Go allocates a right-sized []byte per frame rather than a 64 KiB power-of-two-grown buffer. Like C, it never re… |
| `PROTO-006` | A zero-length or type-less ClientMessage is a protocol error | PARTIAL | low | `internal/connection/handler.go:299-304; internal/storage/se…` | A stock client treats the error as fatal and hangs up itself, so nothing stalls; against a peer that does not, the socket is held until idle_timeout. Mid-session, an unrecognised message is… |
| `PROTO-007` | An unparseable ClientMessage closes the connection with a g… | DIVERGENT | low | `internal/protocol/processor.go:206-208; internal/connection…` | A client with a protobuf-encoding bug sees an unexplained disconnect instead of "invalid ClientMessage", and the server's only record is at debug level. |
| `PROTO-008` | ServerHello is sent unsolicited as the first server message | DIVERGENT | low | `internal/connection/handler.go:263-268,391-397` | None against sudo, sudo_sendlog, or a C relay. Exposure is limited to a spec-conforming third-party client that legitimately omits ClientHello: it would block in its hello read until its ow… |
| `PROTO-009` | ServerHello carries a non-empty `server_id` and `subcommand… | PARTIAL | low | `internal/connection/handler.go:392-395; internal/config/con…` | Requires the operator to write server_id: "" explicitly (omitting the key keeps the default, since YAML unmarshals over a pre-populated defaultConfig). If they do, every sudo command on eve… |
| `PROTO-010` | `redirect` and `servers` are never populated by the C daemon | MATCH | none | `internal/connection/handler.go:392-395` | — |
| `PROTO-011` | ClientHello is optional, valid only in `INITIAL`, and does… | PARTIAL | low | `internal/connection/handler.go:263-268; internal/storage/se…` | No shipped client sends more than one ClientHello or sends one mid-session, so nothing real is affected; a repeated hello would be fatal to a stock client only if the client itself sent it. |
| `PROTO-012` | Six connection states; a new connection starts in `INITIAL` | PARTIAL | informational | `internal/connection/handler.go:255-305` | None on its own — an implementation-structure note, not an external contract. |
| `PROTO-013` | AcceptMessage is rejected in `EXITED` and `FINISHED`, allow… | DIVERGENT | low | `internal/connection/handler.go:256-259; internal/storage/se…` | No shipped client writes anything after its ExitMessage, so in practice this is a stray log_id and a connection held to idle_timeout instead of C's error-and-close. |
| `PROTO-014` | AcceptMessage requires `submit_time` and at least one `Info… | PARTIAL | low | `internal/connection/handler.go:621-632; internal/storage/se…` | Only a third-party client that omits one of those four keys, or sends a relay-mode Accept without submit_time, sees behavior C would not produce. No effect on sudo or sudo_sendlog. |
| `PROTO-015` | Only the first Accept sets `log_io` and moves `INITIAL` → `… | MATCH | none | `internal/connection/handler.go:256-259,638-640; internal/st…` | — |
| `PROTO-016` | A `log_id` is sent after the first Accept with `expect_iobu… | PARTIAL | low | `internal/storage/session.go:928,1331; internal/connection/h…` | Two unsolicited log_id messages C never sends; the stock client discards them. Observable only to a strict protocol validator. |
| `PROTO-017` | Local-mode `log_id` is base64(16-byte UUID ‖ iolog path rel… | MATCH | none | `internal/storage/session.go:315-340,350-351` | — |
| `PROTO-018` | Store-and-forward mode emits a `log_id` of base64(UUID) wit… | MATCH | none | `internal/relay/session.go:127-129` | — |
| `PROTO-019` | RejectMessage requires `submit_time`, a non-empty `reason`,… | PARTIAL | low | `internal/connection/handler.go:518-571` | A malformed Reject is recorded as a content-free audit event instead of being refused; the connection is not closed, but the stock client closes after a reject on its own so nothing hangs. |
| `PROTO-020` | An event-only Accept (`expect_iobufs = false`) never produc… | DIVERGENT | low | `internal/relay/session.go:534-539; internal/connection/hand…` | A stray commit_point written to a socket the client has already stopped reading. Real protocol violation for a stricter client; invisible to sudo. |
| `PROTO-021` | RestartMessage is valid only in `INITIAL` and requires a no… | PARTIAL | medium | `internal/connection/handler.go:574-614; internal/storage/se…` | A peer holding a live log_id can wipe an in-progress session's timing file and every I/O stream to zero bytes with one message C rejects as invalid. Requires a custom client — sudo itself n… |
| `PROTO-022` | A failed restart sends an `error` and closes; a successful… | DIVERGENT | low | `internal/connection/handler.go:576-581,232-241` | Diagnosability only: the client is told "Internal Server Error" rather than "log is already complete, cannot be restarted" etc.; the reason is still in the server log. |
| `PROTO-023` | Restart `log_id` decoding: 16-byte UUID prefix, path with n… | PARTIAL | low | `internal/storage/session.go:1360-1374,1493-1552,1029` | A C daemon sharing a Go-written iolog tree would refuse restarts of those sessions; nothing else reads the file. The bare-16-byte log_id resolves the session dir to log_directory itself, wh… |
| `PROTO-024` | A restart of a completed log is refused; completion is sign… | PARTIAL | low | `internal/storage/session.go:1704,1546-1552` | Only under a non-default file_permissions: 0600 yields 0400 under C but 0440 here, so an operator who tightened permissions finds the completed timing file group-readable. |
| `PROTO-025` | `resume_point` must land exactly on a timing-record boundary | INTENTIONAL | low | `internal/storage/session.go:1405-1466,1559-1574` | Where C answers error and drops the connection, this server accepts and appends, so a misaligned resume duplicates the overlap span in sudoreplay. The {0,0} truncation variant is scored und… |
| `PROTO-026` | A successful restart seeds the connection's elapsed time fr… | MATCH | none | `internal/storage/session.go:1611-1616,1232-1240; internal/r…` | — |
| `PROTO-027` | IoBuffer is valid only in `RUNNING` with `log_io`, and must… | PARTIAL | low | `internal/storage/session.go:938-947,1149-1160; internal/con…` | A buggy or hostile client can write a zero-byte timing record or a mistimed delay into its own log instead of being disconnected; the pre-Accept IoBuffer keeps the connection open where C c… |
| `PROTO-028` | ChangeWindowSize is valid only in `RUNNING` with `log_io` a… | PARTIAL | low | `internal/storage/session.go:1242, internal/relay/session.go…` | An out-of-range delay is written verbatim into the timing file and mis-times sudoreplay for that one session; a pre-Accept winsize is answered with an error but not disconnected. |
| `PROTO-029` | CommandSuspend requires a valid delay and one of exactly fi… | PARTIAL | low | `internal/storage/session.go:1260, internal/storage/session.…` | An out-of-range suspend delay is stored verbatim; the client is told "Internal Server Error" rather than "invalid CommandSuspend"; in relay mode a malformed suspend is deferred to the upstr… |
| `PROTO-030` | Elapsed time accumulates delays from IoBuffer, ChangeWindow… | MATCH | none | `internal/storage/session.go:1201, :1249, :1271; internal/re…` | — |
| `PROTO-031` | `commit_point` is one global cumulative value per connectio… | MATCH | none | `internal/storage/session.go:66, :1232; internal/relay/sessi…` | — |
| `PROTO-032` | `commit_point` carries elapsed time since command start, no… | MATCH | none | `internal/storage/session.go:1232, internal/relay/session.go…` | — |
| `PROTO-033` | Periodic commit cadence: a one-shot 10-second timer, re-arm… | PARTIAL | low | `internal/storage/session.go:1213-1218, internal/relay/sessi…` | Different commit cadence on the wire; no effect on any shipped client's teardown, since the final commit on Exit is unconditional and correct (PROTO-035). |
| `PROTO-034` | I/O logs are flushed to disk before each commit point when… | MATCH | none | `internal/storage/session.go:1189-1198` | — |
| `PROTO-035` | On ExitMessage with `log_io`, a final commit point is sched… | MATCH | none | `internal/storage/session.go:952-960, internal/relay/session…` | — |
| `PROTO-036` | Sending the final commit point transitions `EXITED` → `FINI… | PARTIAL | low | `internal/connection/handler.go:245-251` | With a lingering peer the connection and its max_connections slot are held until idle_timeout instead of being reclaimed at once, and post-Exit bytes are still parsed (PROTO-039). |
| `PROTO-037` | The client requires the final commit point to equal its own… | MATCH | none | `internal/storage/session.go:1232, internal/storage/session_…` | — |
| `PROTO-038` | `sudo_sendlog` accepts a matching commit point in any state… | MATCH | none | `internal/storage/session.go:952-960` | — |
| `PROTO-039` | After an ExitMessage the server stops reading from the clie… | DIVERGENT | low | `internal/connection/handler.go:188-251, internal/storage/se…` | Only reachable by a client that writes after its ExitMessage, which neither sudo nor sudo_sendlog does. |
| `PROTO-040` | ExitMessage is valid only in `RUNNING`; `run_time` is optio… | PARTIAL | low | `internal/connection/handler.go:294-297, internal/storage/se…` | A crafted client can drive Exit->Accept->Exit sequences C refuses, and can put an arbitrary string in log.json's "signal" field. No injection (JSON-encoded) and no effect on sudo. |
| `PROTO-041` | AlertMessage has no state restriction | PARTIAL | low | `internal/connection/handler.go:274-284, internal/storage/se…` | A content-free alert record is persisted where C would answer "invalid AlertMessage" and close. |
| `PROTO-042` | An out-of-order message produces `"state machine error"` an… | DIVERGENT | low | `internal/connection/handler.go:299-304` | A client can spray pre-Accept I/O, collect one error per message, and still open a normal session on the same connection — a violation C treats as fatal. No resource advantage over what C a… |
| `PROTO-043` | Errors are sent at most once, and reads stop the moment one… | PARTIAL | low | `internal/connection/handler.go:232-241, internal/connection…` | A misbehaving client is told "Protocol error" repeatedly on a still-usable connection instead of once and then disconnected. |
| `PROTO-044` | `sudo_logsrvd` never sends an `abort` message | MATCH | none | `internal/connection/handler.go:238` | — |
| `PROTO-045` | A relay converts an upstream `abort` into a downstream `err… | ABSENT | medium | `internal/relay/session.go:764-771` | Against a C upstream that rejects a message: unbounded retry, a cache file that never drains, and no indication of why. Against an upstream that errors without closing: flushFile writes the… |
| `PROTO-046` | In relay mode the server generates no commit points of its… | NA | none | `internal/relay/session.go:509-541` | None — the durability semantics Go offers are identical to C's own store-and-forward mode. The requirement is not applicable to a store-first implementation. |
| `PROTO-047` | A relay forwards client messages as the original bytes, not… | PARTIAL | low | `internal/relay/session.go:835-848, internal/relay/session.g…` | The upstream receives a canonicalised re-encoding rather than the client's bytes. No protobuf consumer can tell; matters only for byte-for-byte auditing of the relayed stream. |
| `PROTO-048` | A relay passes `log_id` through unmodified | NA | none | `internal/relay/session.go:127-129, internal/relay/session.g…` | None as stated. A relay-local log_id and a locally resumed restart are what C's store-first mode does too. |
| `PROTO-049` | EOF from the client closes the connection; it is only "expe… | PARTIAL | low | `internal/connection/handler.go:209-220, internal/connection…` | At the default info log level, a session truncated mid-command looks identical to a clean one in the server's log. |
| `PROTO-050` | Writes carry the server timeout; reads have none | PARTIAL | medium | `internal/connection/handler.go:197-207, internal/protocol/p…` | A deliberately non-reading peer permanently holds a handler goroutine, an fd, and a connection slot; repeated to max_connections (default 10000) the server stops accepting new sessions. C r… |
| `PROTO-051` | On shutdown, I/O-logging connections get one last commit po… | ABSENT | low | `internal/server/server.go:260-321, internal/connection/hand…` | On SIGTERM an active I/O session is dropped without a last commit point, and teardown takes up to main.go's 30 s bound rather than C's 10 s. No behavioral difference for a real sudo client. |
| `PROTO-052` | `InfoMessage` values may be int64, string, string list, or… | PARTIAL | low | `internal/protocol/infomsg.go:19, internal/storage/session.g…` | An InfoMessage carrying an int64 list vanishes from the audit record. No shipped sudo client sends numlistval, so exposure is limited to third-party clients. |
| `PROTO-053` | `TimeSpec.tv_sec` is int64 and `tv_nsec` is int32 | MATCH | none | `pkg/sudosrv_proto/sudo_logsrv.proto:42-45, internal/storage…` | — |

### Configuration Surface & Defaults

Spec: [`03-configuration.md`](03-configuration.md)

| ID | Requirement | Verdict | Sev | Go source | Consequence |
|---|---|---|---|---|---|
| `CONF-001` | Configuration file is INI-style with six recognized sections | INTENTIONAL | low | `internal/config/config.go:34-95,161-171` | An existing /etc/sudo_logsrvd.conf must be hand-translated; nothing a sudo client can observe. |
| `CONF-002` | Key/value lines require `=`, and a section must be open | PARTIAL | medium | `internal/config/config.go:161-171` | A typo such as `listen_addres: 0.0.0.0:30343` loads cleanly and the daemon falls back to the compiled-in default 127.0.0.1:30343 (config.go:134), so remote clients get connection refused; C… |
| `CONF-003` | Whitespace around the key and value is stripped | INTENTIONAL | informational | `internal/config/config.go:161-171` | none observable on the wire or on disk. |
| `CONF-004` | `#` anywhere begins a comment; `;` only at start of line | INTENTIONAL | informational | `internal/config/config.go:161-171 (config-example.yaml uses…` | none beyond a loud parse error if someone pastes INI comments into the YAML. |
| `CONF-005` | Backslash line continuation | INTENTIONAL | informational | `internal/config/config.go:161-171` | none. |
| `CONF-006` | Boolean value grammar | PARTIAL | low | `internal/config/config.go:58-59,93-94 (bool fields decoded…` | `compress: 1` or `compress: yEs` aborts the load with a clear type error (exit 2). Migration friction only — no silent misconfiguration. |
| `CONF-007` | Integer value grammar and range checking | PARTIAL | low | `internal/config/config.go:50,61,64,182-205` | Loud failure for the duration case; silent normalisation for `max_connections: -1` -> 0 (unlimited). Operator-visible only. |
| `CONF-008` | Octal mode grammar for `iolog_mode` | PARTIAL | low | `internal/config/config.go:210-211,350-373` | An operator who obeys the daemon's own warning message gets a config that will not load (exit 2). Loud, but a genuine UX defect. |
| `CONF-009` | Any rejected value aborts the entire configuration load | PARTIAL | low | `internal/config/config.go:101-114; internal/server/server.g…` | On SIGHUP with a typo'd log level, everything else in the file applies and only the level is skipped, visible in the log. |
| `CONF-010` | Defaults are established before parsing; an absent key keep… | MATCH | none | `internal/config/config.go:101-114,130-159; internal/server/…` | — |
| `CONF-011` | Present-but-empty values are setting-specific | PARTIAL | low | `internal/config/config.go:175-212,221-223` | Timeout-polarity effects are charged to CONF-025; otherwise operator-visible only. |
| `CONF-012` | Duplicate keys: last wins, except for the three list-valued… | DIVERGENT | low | `internal/config/config.go:44-45,57,161-171` | No multi-listener or standby-relay configuration is expressible; workarounds exist and nothing fails silently. |
| `CONF-013` | Configuration file location and search path | DIVERGENT | low | `cmd/sudosrv/main.go:78; internal/config/config.go:101-107` | Bare `sudosrv` outside the source tree exits 2 with 'failed to read config file config.yaml'; the shipped unit passes an absolute path. |
| `CONF-014` | A missing default configuration file is not an error; a mis… | INTENTIONAL | low | `internal/config/config.go:97-114` | The daemon cannot start with zero configuration; a config.yaml must always be shipped. |
| `CONF-015` | Command-line option set | DIVERGENT | low | `cmd/sudosrv/main.go:76-100` | An init script written for sudo_logsrvd fails immediately with 'flag provided but not defined'. Loud. |
| `CONF-016` | `-R` random drop percentage | ABSENT | informational | `cmd/sudosrv/main.go:76-100 (no equivalent)` | none. |
| `CONF-017` | `-n` suppresses forking, the pid file, and effective stderr… | INTENTIONAL | low | `cmd/sudosrv/main.go:54-73,240-261; archlinux/sudosrv.service` | Requires a supervisor; cannot self-background for a sysvinit script. |
| `CONF-018` | `SIGHUP` performs a full, transactional reload | PARTIAL | medium | `internal/server/server.go:265-270,327-395` | Editing listen_address together with any other key applies nothing; in-place certificate rotation silently keeps serving the old certificate until the process is restarted. |
| `CONF-019` | Listener reconciliation on reload is by literal address str… | ABSENT | medium | `internal/server/server.go:372-375 (reload refuses the chang…` | Moving or adding a socket requires a full restart, which severs every in-flight session; per the reference client that is not merely a truncated log — see CONF-025's chain, the client treat… |
| `CONF-020` | Log files are reopened on every apply, enabling rotation | ABSENT | low | `cmd/sudosrv/main.go:198 (slog JSON handler on os.Stdout)` | C's documented log-rotation hook has no counterpart; journald/container rotation only. |
| `CONF-021` | `pid_file` is written only at daemonize time | ABSENT | low | `cmd/sudosrv/main.go:54-73 (no pid-file code path anywhere)` | Non-systemd tooling that reads /run/sudo/sudo_logsrvd.pid finds nothing. |
| `CONF-022` | `[server] listen_address` syntax | DIVERGENT | low | `internal/config/config.go:44-45; internal/server/server.go:…` | Startup fails loudly on a C-style address string; a name-valued listen_address yields one address family instead of two. |
| `CONF-023` | Default `listen_address` depends on TLS certificate availab… | DIVERGENT | low | `internal/config/config.go:134 (ListenAddress default '127.0…` | A hand-written minimal config binds loopback only; the address is printed at startup, so the misconfiguration is diagnosable in one log line. |
| `CONF-024` | Wildcard `*` is accepted for `listen_address` but rejected… | DIVERGENT | low | `internal/server/server.go:78,104; internal/relay/session.go…` | `*:30343` fails at startup instead of binding all interfaces; a wildcard upstream_host surfaces as per-session dial failures rather than a refusal to start. |
| `CONF-025` | `[server] timeout` | DIVERGENT | breaking | `internal/config/config.go:50,136,176-184; internal/connecti…` | With shipped defaults, a real sudo client's interactive root shell is killed after 10 (or 30) minutes of terminal inactivity, and the tail of the I/O log is lost. C never disconnects an idl… |
| `CONF-026` | `[server] tcp_keepalive` | PARTIAL | low | `internal/server/server.go:78,191 (net.Listen/Accept default…` | An operator cannot reproduce `tcp_keepalive = false`; Go's 15s idle is also far more aggressive than the system default C inherits. |
| `CONF-027` | `[server] pid_file` | ABSENT | low | `internal/config/config.go:42-53 (no pid_file field)` | No pid file appears; mitigated by the Type=simple unit. |
| `CONF-028` | `[server] server_log` | ABSENT | low | `cmd/sudosrv/main.go:167-206; internal/config/config.go:52` | Daemon warnings cannot be routed to syslog or a file and are JSON rather than sudo-format text. |
| `CONF-029` | `[server] tls_key` default is unconditional | DIVERGENT | low | `internal/config/config.go:47,224-228; internal/server/serve…` | The implicit /etc/ssl/sudo/private/logsrvd_key.pem must be named explicitly; downstream failure behavior matches. |
| `CONF-030` | `[server] tls_cert` default is conditional on file existence | ABSENT | low | `internal/config/config.go:46,130-159 (no cert-existence pro…` | Dropping a certificate at the C default path does nothing; the Go server refuses to start with a clear message instead of silently substituting. |
| `CONF-031` | `[server] tls_cacert` default is conditional on file existe… | ABSENT | low | `internal/server/server.go:100-103 (tls.Config sets only Cer…` | None additional beyond CONF-035; duplicate accounting of the same missing feature. |
| `CONF-032` | `[server] tls_dhparams` | NA | none | `internal/server/server.go:100-103` | Go's crypto/tls implements no finite-field Diffie-Hellman (DHE) cipher suites at all — key exchange is ECDHE (or RSA under TLS 1.2), and there is no API to supply DH parameters. A tls_dhpar… |
| `CONF-033` | `[server] tls_ciphers_v12` and `tls_ciphers_v13` | ABSENT | medium | `internal/server/server.go:100-103; internal/config/config.g…` | A sudo client built against an OpenSSL without TLS 1.3 cannot complete the handshake at all until the operator sets server.tls_min_version: "1.2"; the failure is loud on both ends. Cipher-s… |
| `CONF-034` | `[server] tls_verify` | ABSENT | low | `internal/server/server.go:90-94 (tls.LoadX509KeyPair only)` | A broken chain is discovered at every client handshake rather than once at startup; conversely a self-signed cert needs no opt-out. Diagnostics only. |
| `CONF-035` | `[server] tls_checkpeer` | ABSENT | medium | `internal/server/server.go:100` | A site migrating a sudo_logsrvd.conf that sets tls_checkpeer=true cannot reproduce client-certificate enforcement and has no configuration that restores it. Stock deployments are unaffected. |
| `CONF-036` | `[server] tls_checkhost` | ABSENT | low | `internal/server/server.go:100` | none beyond CONF-035. |
| `CONF-037` | `(tls)` is rejected when OpenSSL support is not compiled in | NA | none | `internal/server/server.go:85` | crypto/tls is unconditionally available in Go; there is no build without it, so the 'TLS not supported' error path and the conditional key table have no analogue. The spec itself states tha… |
| `CONF-038` | `[relay] relay_host` selects relay mode | PARTIAL | medium | `internal/config/config.go:57` | A migrated config that sets upstream_host but omits mode: relay silently stores locally — the operator believes logs are being centralised and they are not. No failover to a standby relay. |
| `CONF-039` | `[relay] timeout` | DIVERGENT | low | `internal/relay/session.go:816` | A slow upstream makes the queue drain later rather than losing anything; one knob restores C's timing. |
| `CONF-040` | `[relay] connect_timeout` | PARTIAL | low | `internal/config/config.go:200` | On a high-latency path the dial is abandoned and retried; delivery is delayed, not lost, unless the operator has also set a finite reconnect_attempts. |
| `CONF-041` | `[relay] retry_interval` | INTENTIONAL | informational | `internal/relay/session.go:366` | Different retry cadence after an outage; no delivery-correctness difference. |
| `CONF-042` | `[relay] relay_dir` and its `incoming`/`outgoing` subdirect… | DIVERGENT | low | `internal/relay/session.go:125` | Operator tooling and one-time migrations need adjusting. The packaged logrotate rule is a real data-loss hazard for relay deployments, but it is a packaging defect rather than a divergence… |
| `CONF-043` | `[relay] store_first` | INTENTIONAL | medium | `internal/relay/session.go:287` | Upstream sees no live sessions; a session whose client dies abnormally is not delivered upstream until this daemon is next restarted. |
| `CONF-044` | `[relay] tcp_keepalive` | MATCH | none | `internal/relay/session.go:776` | — |
| `CONF-045` | Relay TLS string settings fall back to the `[server]` secti… | ABSENT | medium | `internal/relay/session.go:786` | Against an mTLS-requiring upstream every handshake fails and completed sessions accumulate in relay_cache_directory indefinitely — a delivery failure with no configuration that fixes it. A… |
| `CONF-046` | `[relay] tls_verify` is tri-state with `[server]` fallback | ABSENT | low | `internal/relay/session.go:786` | Credential problems surface at flush time rather than at startup. Diagnostics only. |
| `CONF-047` | `[relay] tls_checkpeer` is tri-state with `[server]` fallba… | PARTIAL | low | `internal/relay/session.go:786` | A migrating operator pointing at a self-signed upstream sees loud per-attempt handshake failures and a growing cache until they set tls_skip_verify: true; no silent weakening. |
| `CONF-048` | `[relay] tls_checkhost` is documented but does not exist | NA | none | `internal/config/config.go:161` | The configuration is YAML unmarshalled into a fixed struct (unmarshalConfig → yaml.Unmarshal without KnownFields), not an INI key table, so there is no 'illegal key' concept and no relay TL… |
| `CONF-049` | `[iolog] iolog_dir` and the derived `iolog_base` | DIVERGENT | low | `internal/storage/session.go:315` | No reference-client scenario. Restart resumption breaks only for a client that implements RestartMessage (sendlog, or a third-party client) against an iolog_dir outside log_directory; and `… |
| `CONF-050` | `[iolog] iolog_file` | MATCH | none | `internal/storage/session.go:648` | — |
| `CONF-051` | `[iolog] iolog_compress` | MATCH | none | `internal/storage/session.go:1183` | — |
| `CONF-052` | `[iolog] iolog_flush` | MATCH | none | `internal/storage/session.go:1189` | — |
| `CONF-053` | `[iolog] iolog_user` | ABSENT | low | `internal/storage/session.go:354` | No way to target a dedicated log owner; ownership must be arranged externally. |
| `CONF-054` | `[iolog] iolog_group` always wins over `iolog_user`'s prima… | ABSENT | low | `internal/storage/session.go:354` | Group ownership is the daemon's egid. |
| `CONF-055` | `[iolog] iolog_mode` and the derived directory mode | DIVERGENT | medium | `internal/config/config.go:153` | Session transcripts, log.json and timing are group-readable and session directories group-traversable where sudo_logsrvd makes them owner-only; on a host where the daemon's gid is shared, t… |
| `CONF-056` | `[iolog] log_passwords` | MATCH | none | `internal/storage/session.go:1168` | — |
| `CONF-057` | `[iolog] passprompt_regex` | PARTIAL | medium | `internal/storage/filter.go:46` | An operator whose prompt is unmatched cannot add a pattern and the secret lands verbatim in ttyin; a prompt rendered 'Password' + newline or 'password>' is masked by sudo_logsrvd and record… |
| `CONF-058` | `[eventlog] log_type` | ABSENT | medium | `internal/config/config.go:34` | SIEM forwarders and syslog-based alerting see nothing from this daemon; the only central record is the per-session directory tree. (Correction to the note: the operational slog stream goes… |
| `CONF-059` | `[eventlog] log_format` | ABSENT | low | `internal/config/config.go:34` | subsumed by CONF-058. |
| `CONF-060` | `[eventlog] log_exit` | ABSENT | low | `internal/config/config.go:34` | subsumed by CONF-058. |
| `CONF-061` | `[syslog] maxlen` | ABSENT | low | `internal/config/config.go:34` | subsumed by CONF-058. |
| `CONF-062` | `[syslog] facility` and `[syslog] server_facility` | ABSENT | low | `internal/config/config.go:34` | syslog routing rules written for sudo_logsrvd capture nothing. |
| `CONF-063` | `[syslog] accept_priority`, `reject_priority`, `alert_prior… | ABSENT | low | `internal/config/config.go:34` | subsumed by CONF-058. |
| `CONF-064` | `[logfile] path` | ABSENT | low | `internal/config/config.go:34` | subsumed by CONF-058. |
| `CONF-065` | `[logfile] time_format` | ABSENT | low | `internal/config/config.go:34` | subsumed by CONF-058. |
| `CONF-066` | `maxseq` clamps out-of-range values instead of rejecting th… | PARTIAL | low | `internal/storage/session.go:810` | Unreachable at 2.18 billion sessions; Go's refusal is the safer of the two outcomes. |
| `CONF-067` | Configuration values are pushed into the shared iolog/event… | NA | none | `internal/server/server.go:327` | The spec explicitly records this as an internal structuring concern with no external contract. Go has no shared iolog/eventlog library holding process-global state that must be reset and re… |
| `CONF-068` | A failed apply can still leave the syslog facility changed | NA | none | `internal/server/server.go:327` | The spec states this is an ordering artifact recorded so that an implementation performing all validation up front is not marked divergent. Server.reload() does exactly that: LoadConfigRequ… |

### Local I/O Log Storage Format

Spec: [`04-local-storage.md`](04-local-storage.md)

| ID | Requirement | Verdict | Sev | Go source | Consequence |
|---|---|---|---|---|---|
| `IOLOG-001` | Session path is `expand(iolog_dir)` + `/` + `expand(iolog_f… | PARTIAL | informational | `internal/storage/session.go:684-705` | None reachable. Two servers given a template containing '//' write to the same on-disk location under different path strings. |
| `IOLOG-002` | Exactly seven `%{...}` escapes are supported by logsrvd | PARTIAL | informational | `internal/storage/session.go:646-682` | None. Templates written for the sudo_logsrvd escape set expand identically on both servers. |
| `IOLOG-003` | `%{seq}` is not available in `iolog_dir` | DIVERGENT | low | `internal/storage/session.go:684` | A hand-written template with %{seq} in iolog_dir yields an XX/XX/XX hierarchy in the directory portion here and a literal directory there. If both dir and file carry it, the resulting depth… |
| `IOLOG-004` | Escape substitutions replace `/` with `_` | MATCH | none | `internal/storage/session.go:178-180,646-666` | — |
| `IOLOG-005` | `%{command}` expands to the command's base name | PARTIAL | informational | `internal/storage/session.go:666` | None. A command-less AcceptMessage creates a session directory here rather than being refused, which is a subset of IOLOG-051. |
| `IOLOG-006` | Remaining `%` sequences are passed to `strftime(3)`; `%%` c… | PARTIAL | medium | `internal/storage/session.go:208-271,691-692` | iolog_dir: /var/log/sudo-io/%F creates a literal directory named '%F' on this server and '2026-08-02' on sudo_logsrvd — the tree layout an operator configured is silently not the tree they… |
| `IOLOG-007` | Leading duplicate slashes collapse; trailing slashes are tr… | PARTIAL | informational | `internal/storage/session.go:702` | None reachable. |
| `IOLOG-008` | Both expanded components are rejected if they contain a `..… | MATCH | none | `internal/storage/session.go:293-300,698-700` | — |
| `IOLOG-009` | A path ending in `XXXXXX` is created with `mkdtemp(3)` sema… | ABSENT | medium | — | An operator porting a sudo_logsrvd.conf that uses the documented XXXXXX uniqueness idiom gets one literal directory per template value; the first session succeeds and every subsequent one f… |
| `IOLOG-010` | The `seq` file is a 7-byte, zero-padded, uppercase base-36… | PARTIAL | medium | `internal/storage/session.go:761-882` | Only in a mixed C+Go fleet writing one iolog tree: the two counters advance independently and can hand two sessions the same XX/XX/XX directory, which Go then refuses via O_EXCL (IOLOG-049)… |
| `IOLOG-011` | The `seq` file is serialised with a blocking POSIX record l… | DIVERGENT | low | `internal/storage/session.go:784` | In that mixed local-tree case both readers can see the same counter and allocate the same session directory, which Go then refuses at the O_EXCL uuid write and loses. The lock is correctly… |
| `IOLOG-012` | `%{seq}` expands to a three-level `XX/XX/XX` hierarchy | MATCH | none | `internal/storage/session.go:841-849` | — |
| `IOLOG-013` | A sequence number is allocated at most once per session, la… | DIVERGENT | low | `internal/storage/session.go:632` | A seq file is created and incremented in log_directory for deployments whose template never references %{seq}, and every session pays an unnecessary lock+fsync. No wrong data. |
| `IOLOG-014` | Directory creation: parents plus final component, with owne… | PARTIAL | low | `internal/storage/session.go:354,414,770` | On a pre-existing tree whose directories have the wrong owner or mode — a C-to-Go migration, essentially — C silently repairs and continues while this server leaves it as found and the subs… |
| `IOLOG-015` | File and directory modes are derived from `iolog_mode` | DIVERGENT | low | `internal/config/config.go:91-92,150-156,194-211,321-336` | An operator porting a sudo_logsrvd.conf cannot carry iolog_mode across and has to restate it as two separate keys; the default on-disk posture is functionally the same as C's in the package… |
| `IOLOG-016` | Every created file and directory is chowned to `iolog_user`… | ABSENT | low | — | A deployment that wants the tree owned by an account other than the daemon's cannot express it, and cannot drop privileges and hand the tree over. Files simply carry the daemon's ownership. |
| `IOLOG-017` | File creation temporarily relaxes the process umask | ABSENT | low | `internal/storage/session.go:277-289,1053,1084,1119` | An operator running the daemon under a restrictive umask (0077) gets 0600 files despite configuring 0640, with nothing in the logs explaining why. Usability, not security. |
| `IOLOG-018` | All I/O log opens use `O_NOFOLLOW`, with `EACCES` fallbacks | PARTIAL | low | `internal/storage/session.go:150-171,357,1053,1084` | On a root-squashing NFS export with iolog_user configured, C recovers and this server fails the session. The documented caveat at session.go:161-162 — os.MkdirAll is still path-based, so a… |
| `IOLOG-019` | On accept, seven files are created; `stdin`/`ttyin` are lazy | PARTIAL | low | `internal/storage/session.go:1029-1070,1076-1096,1160` | A deployment with log_servers but no log_input/log_output accumulates one two-file directory per sudo invocation in the iolog tree and burns one seq value each, and sudoreplay -l lists them… |
| `IOLOG-020` | The `uuid` file holds exactly 36 bytes with no terminator | DIVERGENT | low | `internal/storage/session.go:1029,446` | A C sudo_logsrvd cannot resume a Go-written session, which matters only if a sendlog/relay-originated restart is aimed at a mixed tree. No sudo client and no sudoreplay invocation observes… |
| `IOLOG-021` | The legacy `log` file has a fixed three-line colon-delimite… | DIVERGENT | low | `internal/storage/session.go:1035-1046,188-199` | An operator who reads the legacy `log` file by hand sees the command path without its arguments. sudoreplay is unaffected. |
| `IOLOG-022` | `log.json` is pretty-printed with 4-space indent and wrappe… | DIVERGENT | low | `internal/storage/session.go:1107-1146` | log.json is not byte-comparable with a C-produced file and shows \u0026 for '&' when read raw. sudoreplay parses it correctly. |
| `IOLOG-023` | `log.json` field set and order are fixed | DIVERGENT | low | `internal/storage/session.go:991-1023,1112` | log.json has different key ordering and carries six Go-only keys that produce debug warnings in sudoreplay. Required-entry satisfaction is delegated to the client, which for real sudo alway… |
| `IOLOG-024` | The `timestamp` object holds the client's submit time in se… | DIVERGENT | high | `internal/storage/session.go:1022-1023,1673-1680` | sudoreplay -l reports every completed Go-written session as starting at the moment the command exited on the server (off by the whole command duration plus clock skew) and every aborted ses… |
| `IOLOG-025` | `log.json` is written once at accept time and not updated d… | DIVERGENT | low | `internal/storage/session.go:1301,1325,1350,1649` | Marshal+fsync+rename per alert or sub-command with quadratic total work in a long intercepted session, and log.json's mtime/inode churn during a live session so it cannot be used as a sessi… |
| `IOLOG-026` | Exit info is appended to `log.json` by overwriting its fina… | DIVERGENT | low | `internal/storage/session.go:1658-1694,1107-1146` | Cosmetic ordering/content differences in log.json plus one behavioural quirk: a log.json the operator deliberately deleted mid-session is recreated from in-memory metadata at exit, where C… |
| `IOLOG-027` | Completion is marked by clearing the write bits on `timing` | PARTIAL | low | `internal/storage/session.go:1704` | An operator who narrows file_permissions to 0600 still gets a 0440 timing file; it is the one file in the session whose mode ignores the configuration. |
| `IOLOG-028` | I/O timing records are `"%d %lld.%09d %zu\n"` | MATCH | none | `internal/storage/session.go:1203-1207` | — |
| `IOLOG-029` | Timing event type numbers are fixed | MATCH | none | `internal/storage/session.go:88-97,100-109` | — |
| `IOLOG-030` | Window-size records are `"%d %lld.%09d %d %d\n"` with event… | MATCH | none | `internal/storage/session.go:1242-1257` | — |
| `IOLOG-031` | Suspend records are `"%d %lld.%09d %s\n"` with event 7 and… | PARTIAL | low | `internal/storage/session.go:119-125,1260-1281` | None for a real sudo client. A non-conforming client sending any other signal name kills the session and loses subsequent I/O where C would record the line and fail only at replay (iolog_ti… |
| `IOLOG-032` | Delays are per-record increments; the server accumulates el… | MATCH | none | `internal/storage/session.go:1201,1249,1271,1224-1240` | — |
| `IOLOG-033` | Data is written to the stream file before the timing record | MATCH | none | `internal/storage/session.go:1189-1211` | — |
| `IOLOG-034` | The `timing` file is exclusively locked while open for writ… | ABSENT | low | `internal/storage/session.go:1053,1577` | Two concurrent restarts (sendlog, a relay chain, or a client deliberately replaying its log_id) each seek/truncate the same timing file and interleave writes, corrupting it silently where C… |
| `IOLOG-035` | Timing lines must be newline-terminated and shorter than `L… | MATCH | none | `internal/storage/session.go:1203-1207,1251,1274` | — |
| `IOLOG-036` | Stream files contain raw, unframed bytes | MATCH | none | `internal/storage/session.go:1167-1191` | — |
| `IOLOG-037` | gzip compression applies to the six `IOFD_*` files only | PARTIAL | low | `internal/storage/session.go:1092-1094,1053,1209,1555-1557` | A compressed Go tree is slightly larger than a compressed C tree and not byte-comparable. Replay is unaffected. |
| `IOLOG-038` | `iolog_flush` controls per-write flushing; commit points al… | MATCH | none | `internal/storage/session.go:1189-1198,1209` | — |
| `IOLOG-039` | The password filter applies only to `ttyin`/`ttyout` | DIVERGENT | medium | `internal/storage/session.go:1167-1179` | Any session where the command's stdout carries a prompt-shaped string has its recorded stdin overwritten with asterisks up to the next newline — e.g. `cat app.conf \| sudo tee /etc/app.conf… |
| `IOLOG-040` | Masking semantics: prompt on ttyout arms it, ttyin is starr… | DIVERGENT | medium | `internal/storage/filter.go:85-149` | Two opposite errors in the same filter. Over-masking: after a spurious match (e.g. `grep password /etc/*.conf`) Go masks every ttyin byte until the next Enter, so the following command line… |
| `IOLOG-041` | Default passprompt regex, and the filter's state is process… | DIVERGENT | low | `internal/storage/filter.go:46-65,31-36; internal/config/con…` | A site with a custom PAM or vendored-helper prompt that Go's built-ins miss cannot add a pattern; the secret is recorded in plaintext with no remediation. The per-session filter instance (s… |
| `IOLOG-042` | `log_id` is base64 of the 16-byte UUID concatenated with th… | PARTIAL | low | `internal/storage/session.go:315-340,350-351,1360-1374` | For an iolog_dir outside log_directory the log_id encodes an absolute path and NewRestartSession rejects it at session.go:1500 — visible only to sendlog or a relay chain. The 16-byte/empty-… |
| `IOLOG-043` | Restart verifies the decoded UUID against the session's `uu… | PARTIAL | low | `internal/storage/session.go:1536-1543` | Go accepts both 36- and 37-byte uuid files; C accepts only 36. The anti-path-guessing property the check exists for is fully preserved on both sides. |
| `IOLOG-044` | A completed log cannot be restarted | MATCH | none | `internal/storage/session.go:1546-1552` | — |
| `IOLOG-045` | Uncompressed resume seeks; it does not truncate; the resume… | DIVERGENT | high | `internal/storage/session.go:1405-1490,1567-1609` | A client that holds a log_id for an unfinalized session can reconnect, send RestartMessage{resume_point:[0,0]}, and the server truncates timing, stdin, stdout, stderr, ttyin and ttyout to z… |
| `IOLOG-046` | Compressed logs are resumed by rewriting prefixes through a… | INTENTIONAL | low | `internal/storage/session.go:1554-1557` | With compress: true a sendlog- or relay-originated RestartMessage gets 'Internal Server Error' and a dropped connection instead of a rewritten-and-resumed log. No effect on sudo itself. |
| `IOLOG-047` | Restart does not rewrite `log`/`log.json`, but does finaliz… | PARTIAL | low | `internal/storage/session.go:1585-1589,1637-1651,1658-1710` | log.json gains an extra top-level key and a new mtime/inode on each resume. Only observable on the restart path, which no sudo client exercises. |
| `IOLOG-048` | An aborted session leaves its files in place, writable and… | MATCH | none | `internal/storage/session.go:1714-1759; internal/connection/…` | — |
| `IOLOG-049` | Re-used session directories are truncated per-file, not cle… | DIVERGENT | medium | `internal/storage/session.go:278,1029,1046,1053,1084` | Directory re-use aborts the session instead of overwriting it: the uuid write returns EEXIST, initialize's deferred cleanup runs, and handler.go:236-240 sends 'Internal Server Error' and cl… |
| `IOLOG-050` | Sub-commands in a session reuse the parent's `iolog_path` a… | PARTIAL | low | `internal/storage/session.go:1311-1356; internal/connection/…` | An auditor cannot tell where in the replay stream a sub-command began without subtracting timestamps by hand, and the wire carries an unsolicited LogId the client discards. |
| `IOLOG-051` | Required AcceptMessage info keys, and the defaults applied… | DIVERGENT | low | `internal/storage/session.go:988-1023` | Reduced to hostile-client robustness: a non-conforming client that omits submituser/command/runuser produces a session directory whose log.json fails iolog_parse_loginfo_json, so sudoreplay… |

### Relay Mode & Store-and-Forward

Spec: [`05-relay.md`](05-relay.md)

| ID | Requirement | Verdict | Sev | Go source | Consequence |
|---|---|---|---|---|---|
| `RELAY-001` | Configuring any relay host puts every connection into relay… | MATCH | none | `internal/connection/handler.go:643-670, internal/connection…` | — |
| `RELAY-002` | `store_first` selects the journal path and is forced off wi… | INTENTIONAL | medium | `internal/relay/session.go:149-202, internal/relay/session.g…` | An operator porting a default C relay config gets spooling semantics instead of streaming: the client is acknowledged from local disk before the upstream has seen a byte, and no configurati… |
| `RELAY-003` | In real-time relay mode the downstream `ServerHello` is def… | ABSENT | medium | `internal/connection/handler.go:263-268, internal/connection…` | A client never observes upstream-connect latency or failure; the hello is unconditionally immediate. Same wire behaviour as C store_first=true, which is a supported C configuration. |
| `RELAY-004` | The `ServerHello` sent downstream is the relay's own, not t… | PARTIAL | low | `internal/connection/handler.go:391-397, internal/relay/sess…` | A non-hello handshake reply from a hostile or broken upstream is treated as a successful handshake and the whole cached session is pushed into it. |
| `RELAY-005` | The relay synthesizes its own upstream `ClientHello` | PARTIAL | informational | `internal/relay/session.go:799, internal/connection/handler.…` | A different relay identity string in the upstream's debug log. Nothing else. |
| `RELAY-006` | Relay hosts are tried in configuration order until one conn… | ABSENT | low | `internal/config/config.go:57, internal/relay/session.go:775…` | No relay failover can be configured. At single-upstream parity there is no behavioural difference. |
| `RELAY-007` | Upstream sockets are non-blocking, with optional TCP keepal… | MATCH | none | `internal/relay/session.go:776` | — |
| `RELAY-008` | `relay.connect_timeout` bounds the TCP connect and the TLS… | PARTIAL | low | `internal/relay/session.go:776-791, internal/config/config.g…` | An upstream needing more than 5 s to accept+handshake fails that flush attempt and is deferred to the retry loop; no data loss, and no way to express 'wait indefinitely'. |
| `RELAY-009` | A failed upstream TLS handshake falls through to the next r… | PARTIAL | low | `internal/relay/session.go:781-795, internal/relay/session.g…` | An untrusted/expired upstream certificate produces an endless retry loop visible only as a server-side warning. |
| `RELAY-010` | Exhausting the relay list produces a specific error to the… | ABSENT | high | `internal/relay/session.go:239-283, internal/connection/hand…` | With a permanently unreachable upstream, a C-default relay blocks privileged command execution (fail-closed on audit); the Go relay permits it indefinitely and spools locally, with only a s… |
| `RELAY-011` | Client messages are forwarded byte-for-byte, re-framed only | PARTIAL | low | `internal/relay/session.go:835-848, internal/relay/session.g…` | Byte-different but semantically identical payloads upstream; only a byte-comparing or signature-verifying upstream would notice, and none exists in the reference. |
| `RELAY-012` | Forwarding order is the receive order | MATCH | none | `internal/relay/session.go:311-343, internal/relay/session.g…` | — |
| `RELAY-013` | No local logging occurs in relay mode | MATCH | none | `internal/connection/handler.go:656-666, internal/connection…` | — |
| `RELAY-014` | In real-time relay mode the server never generates a `commi… | ABSENT | informational | `internal/relay/session.go:509-539` | none |
| `RELAY-015` | Upstream `commit_point` values are forwarded verbatim | ABSENT | low | `internal/relay/session.go:764-771, internal/relay/session.g…` | 'Committed' means 'cached on the relay's disk', not 'durable at the audit sink' — same as C store_first. |
| `RELAY-016` | A `commit_point` received before `RUNNING` is a protocol er… | ABSENT | informational | `internal/relay/session.go:764-771` | none beyond a missing warning line |
| `RELAY-017` | The first `commit_point` after `EXITED` finalizes the sessi… | ABSENT | low | `internal/relay/session.go:534-539, internal/relay/session.g…` | Flush completion is declared on write-completion rather than on upstream acknowledgement; see RELAY-042. |
| `RELAY-018` | Upstream `log_id` is forwarded unmodified | ABSENT | informational | `internal/relay/session.go:126-129, internal/relay/session.g…` | none — the behaviour matches C's store_first log_id contract exactly |
| `RELAY-019` | `log_id` handling is skipped when there is no downstream cl… | DIVERGENT | high | `internal/relay/session.go:764-771, internal/connection/hand…` | A relay deployment with event-only sudoers (log_servers without log_input/log_output — a normal configuration) accumulates cache files that can never be retired, one immortal goroutine per… |
| `RELAY-020` | Upstream `error` messages are relayed and stop relay I/O | DIVERGENT | high | `internal/relay/session.go:764-771, internal/relay/session.g…` | An upstream that rejects the session (disk full, iolog permission failure, quota) has its error read and discarded; for any session small enough that the remaining records fit in the socket… |
| `RELAY-021` | Upstream `abort` messages are relayed but do not stop relay… | ABSENT | low | `internal/relay/session.go:764-771` | Only a non-sudo_logsrvd upstream could trigger it, and the resulting behaviour is the RELAY-020 outcome. |
| `RELAY-022` | Premature upstream EOF is reported to the client | DIVERGENT | medium | `internal/relay/session.go:738-743, internal/relay/session.g…` | Upstream half-close or death mid-replay is invisible to Go; the concrete loss is counted once, at RELAY-042. |
| `RELAY-023` | Relay read and write events carry no timeout | DIVERGENT | low | `internal/relay/session.go:816-830, internal/relay/session.g…` | An unresponsive-but-connected upstream is abandoned after 5 s and retried instead of stalling forever; the ceiling is not independently configurable. |
| `RELAY-024` | 2 MB message-size ceiling applies in both directions | MATCH | none | `internal/protocol/processor.go:20, internal/protocol/proces…` | — |
| `RELAY-025` | A `RestartMessage` is forwarded verbatim in real-time relay… | INTENTIONAL | medium | `internal/relay/session.go:149-202, internal/connection/hand…` | The record the upstream finally stores duplicates the overlap window, so sudoreplay replays up to ~10 s of output twice after a client restart. |
| `RELAY-026` | Journal files live in `relay_dir/{incoming,outgoing}/<uuid>` | DIVERGENT | medium | `internal/relay/session.go:116-129, internal/relay/session.g…` | On a restart with clients reconnecting, a freshly started session can be flushed-and-deleted underneath itself, losing that session's I/O; and two daemons sharing a cache directory race wit… |
| `RELAY-027` | Journal files are created mode 0600 and `O_NOFOLLOW` | PARTIAL | low | `internal/relay/session.go:288-290, internal/relay/session.g…` | Symlink-through of raw keystroke data is possible only for a principal who already controls the 0700 cache directory. |
| `RELAY-028` | Each journal is held under a non-blocking exclusive file lo… | ABSENT | low | `internal/relay/session.go:290` | No claim mechanism for the in-process flush path; the interleaved-writers/delete-underneath scenario needs a stalled-but-open connection and is not produced by a stock client's normal resta… |
| `RELAY-029` | Journal record framing matches the wire protocol exactly | MATCH | none | `internal/relay/session.go:835` | — |
| `RELAY-030` | `ClientHello` and `RestartMessage` are never journalled | MATCH | none | `internal/connection/handler.go:263,290; internal/relay/sess…` | — |
| `RELAY-031` | Journal writes are buffered and never fsynced | INTENTIONAL | informational | `internal/relay/session.go:295-305` | One extra fsync per session; strictly more durable than C at end-of-session. |
| `RELAY-032` | `store_first` answers the client immediately, without the u… | MATCH | none | `internal/connection/handler.go:391; internal/relay/session.…` | — |
| `RELAY-033` | `store_first` `log_id` is base64 of the journal UUID with a… | MATCH | none | `internal/relay/session.go:129, internal/relay/session.go:49…` | — |
| `RELAY-034` | A sub-command Accept during a journalled session reuses the… | DIVERGENT | high | `internal/relay/session.go:489-493` | With sudo intercept/log_subcmds enabled against a Go relay, every intercepted sub-command is silently dropped from the relayed audit record — the terminal server's log for that session show… |
| `RELAY-035` | `store_first` generates local commit points on the normal s… | PARTIAL | low | `internal/relay/session.go:516-539` | An idle interactive session gets no periodic ack, so a later RestartMessage carries an older resume_point; plus one harmless extra commit on the first I/O event (lastCommitTime starts at th… |
| `RELAY-036` | `journal_restart()` resolves the log_id to `incoming/<uuid>… | PARTIAL | low | `internal/relay/session.go:166-202; internal/connection/hand…` | Cosmetic error-text divergence on an unresumable restart; the theoretical gaps are not reachable from a stock sudo client. |
| `RELAY-037` | Journal seek must land exactly on the resume point | INTENTIONAL | low | `internal/relay/session.go:149-202; internal/relay/session_t…` | A restart whose resume_point falls between records succeeds instead of failing, and the overlap window is duplicated upstream (bounded by the 10 s commit interval). |
| `RELAY-038` | After a restart, journal writes overwrite from the seek pos… | INTENTIONAL | low | `internal/relay/session.go:290 (O_APPEND); internal/relay/se…` | No trailing-garbage records upstream; duplicated overlap instead. |
| `RELAY-039` | `ExitMessage` finalizes the journal and moves it to `outgoi… | INTENTIONAL | low | `internal/relay/session.go:334-337, 295-305, 231-266; CLAUDE…` | An operator cannot distinguish open from completed sessions by directory; all files share one flat *.log namespace. |
| `RELAY-040` | A finished `store_first` session relays immediately, not vi… | MATCH | none | `internal/relay/session.go:223-279` | — |
| `RELAY-041` | Journal replay reads the journal as if it were a client soc… | PARTIAL | informational | `internal/relay/session.go:731-773, 851-869` | A journal whose tail was lost to a crash is relayed partially and retired rather than retried forever. |
| `RELAY-042` | A successful replay unlinks the journal and pumps the queue | DIVERGENT | high | `internal/relay/session.go:738-753, 709-729` | A successful write(2) into the local TCP send buffer is treated as durable delivery. If the upstream dies or fails to persist after the bytes leave Go's socket, the cache file is already un… |
| `RELAY-043` | Journals are re-queued only from the `CONNECTING` state | INTENTIONAL | low | `internal/relay/session.go:239-283; CLAUDE.md 'Two-Phase Ope…` | Transient upstream failures are recovered within the running process instead of waiting for a restart. |
| `RELAY-044` | Retry uses a fixed interval, with no backoff, jitter or att… | PARTIAL | medium | `internal/relay/session.go:366-383, 233-238; internal/config…` | A permanently-rejecting or flapping upstream is hammered with back-to-back connections and replay attempts by every affected session goroutine, where C would retry at a fixed 30 s or not at… |
| `RELAY-045` | The outgoing queue drains one journal at a time | INTENTIONAL | low | `internal/relay/session.go:619-649; CLAUDE.md 'Orphaned File…` | Up to five simultaneous upstream connections during startup recovery, and recovery completes in one pass. |
| `RELAY-046` | The queue is skipped entirely when no relay host is configu… | MATCH | none | `internal/server/server.go:158-164; internal/config/config.g…` | — |
| `RELAY-047` | Startup recovery scans only `outgoing/`, only UUID-named en… | INTENTIONAL | low | `internal/relay/session.go:584-617; internal/relay/session.g…` | Interrupted sessions are eventually delivered but arrive without an ExitMessage (no exit status or run time upstream); stray files are retried every startup and .delivered files accumulate. |
| `RELAY-048` | Startup fails if the outgoing directory cannot be opened | MATCH | none | `cmd/sudosrv/main.go:211-237, 136-138, 264-284` | — |
| `RELAY-049` | An upstream that permanently rejects a journal causes indef… | DIVERGENT | medium | `internal/relay/session.go:758-771` | A rejected session is retained and retried for large sessions but silently deleted for short ones — the outcome is not deterministic and there is no signal either way beyond an INFO line. |
| `RELAY-050` | Reject-only and alert-only sessions are lost in `store_firs… | MATCH | none | `internal/connection/handler.go:518-522, 274-284` | — |
| `RELAY-051` | Shutdown does not drain relay connections | MATCH | none | `internal/server/server.go:260-322; internal/relay/session.g…` | — |
| `RELAY-052` | `SIGHUP` does not disturb in-flight relay connections | MATCH | none | `internal/server/server.go:327-395; internal/connection/hand…` | — |
| `RELAY-053` | `SIGUSR1` dumps the outgoing queue | ABSENT | low | `internal/server/server.go:262` | An operator cannot ask a running daemon for its relay state; cache files left by previous runs are not exposed anywhere. |
| `RELAY-054` | Store-and-forward is at-least-once; real-time relay is at-m… | PARTIAL | high | `internal/relay/session.go:738-753, 239-283` | The net guarantee is 'at-least-once except across the final acknowledgement window, where it degrades to at-most-once': kill the upstream at the wrong moment and the session exists in neith… |

### TLS, Authentication & Security Posture

Spec: [`06-tls-and-security.md`](06-tls-and-security.md)

| ID | Requirement | Verdict | Sev | Go source | Consequence |
|---|---|---|---|---|---|
| `TLS-001` | TLS is transport-only and selected per listener, never nego… | MATCH | none | `internal/server/server.go:78-110` | — |
| `TLS-002` | Default ports are 30343 (plaintext) and 30344 (TLS) | PARTIAL | low | `internal/config/config.go:134, internal/server/server.go:78` | Two loud, not silent, failures remain: a portless address copied from sudo_logsrvd.conf (listen_address: "0.0.0.0") makes net.Listen fail with 'missing port in address' and the process exit… |
| `TLS-003` | Exactly one SSL_CTX per role, built at config load and shar… | PARTIAL | low | `internal/server/server.go:85-110, internal/server/server.go…` | Certificate rotation requires a full process restart (dropping every in-flight session) rather than SIGHUP; a rotated cert-at-the-same-path is silently ignored by a reload that logs 'Config… |
| `TLS-004` | Minimum protocol version is TLS 1.2; no maximum version is… | INTENTIONAL | medium | `internal/config/config.go:22-31, internal/config/config.go:…` | A sudo built against an OpenSSL/LibreSSL without TLS 1.3 handshake-fails against a default sudosrv-go. Narrow (any OpenSSL 1.1.1+ negotiates 1.3 fine) and remediable with tls_min_version: "… |
| `TLS-005` | TLS 1.2 cipher list defaults to `HIGH:!aNULL`, with non-fat… | DIVERGENT | low | `internal/server/server.go:100-103` | Operator-visible only: a site with a cipher policy cannot express it in config, and a hypothetical peer restricted to HIGH suites Go does not implement (plain AES256-SHA, DHE_*, CAMELLIA) w… |
| `TLS-006` | TLS 1.3 ciphersuites default to `TLS_AES_256_GCM_SHA384` on… | DIVERGENT | low | `internal/server/server.go:100-103` | A packet capture shows a different negotiated suite than sudo_logsrvd, and a site mandating AES-256 for sudo transcripts cannot enforce it. No handshake fails. |
| `TLS-007` | CA bundle: `tls_cacert` if set, otherwise the system defaul… | ABSENT | medium | — | An operator with a private CA cannot supply it: mutual TLS is unreachable inbound (see TLS-015), and a private-CA upstream on the relay leg can only be reached by disabling verification who… |
| `TLS-008` | The CA bundle is also published as the acceptable-client-CA… | ABSENT | informational | — | none — fully subsumed by TLS-015; nothing observable while client certificates are not requested. |
| `TLS-009` | Certificate chain and private key loading; key defaults to… | MATCH | none | `internal/server/server.go:90-94` | — |
| `TLS-010` | `tls_verify` (default true) self-verifies the daemon's own… | ABSENT | medium | — | sudosrv-go starts cleanly with an expired certificate or a truncated issuer chain and logs 'Started TLS listener'. Every TLS client then fails the handshake and (sudoers does not abort the… |
| `TLS-011` | Diffie-Hellman parameters are optional and load failure is… | NA | none | — | crypto/tls implements no finite-field Diffie-Hellman ciphersuites (ECDHE only), so there is no DH parameter file to load and no failure mode to be lenient about; the setting has no meaning… |
| `TLS-012` | Built-in TLS paths, and the "only if it exists" rule for ca… | ABSENT | low | `internal/config/config.go:130-159, internal/config/config.g…` | Operator-visible only: TLS is never implicitly available, so a logsrvd host that relied on the default certificate paths yields a plaintext-only Go server until the paths are written into c… |
| `TLS-013` | A readable default certificate silently enables a TLS liste… | DIVERGENT | low | `internal/server/server.go:77-110, internal/config/config.go…` | An operator who sets tls_cert_file/tls_key_file but forgets listen_address_tls gets plaintext-only; clients using log_servers="host:30344(tls)" get connection refused. Diagnosable from the… |
| `TLS-014` | Client certificates are not requested by default | MATCH | none | `internal/server/server.go:100-103` | — |
| `TLS-015` | When `tls_checkpeer` is true, a client certificate is manda… | ABSENT | medium | — | A site that authenticates sudo hosts by client certificate cannot migrate — sudosrv-go accepts any TLS peer and the protocol carries no other credential. A missing opt-in hardening mode, ex… |
| `TLS-016` | The verify callback propagates pre-verification failure and… | ABSENT | low | — | none beyond TLS-015: an untrusted, expired or revoked client certificate is never examined, which is also true of stock sudo_logsrvd. |
| `TLS-017` | `tls_checkhost` (default true) reverse-resolves the peer IP… | ABSENT | informational | — | none — the claim that operators lose host-name attribution is refuted; C's only externally visible artefact of this code path is a 'unable to resolve host <ip>' warning. |
| `TLS-018` | Identity matching follows RFC 6125 §6.4.4: SAN first, CN on… | PARTIAL | low | `internal/relay/session.go:786-788` | An upstream presenting a legacy SAN-less certificate is rejected with 'x509: certificate relies on legacy Common Name field'. No log data is lost — reconnect_attempts defaults to -1 and the… |
| `TLS-019` | SAN dNSName and CN matching: case-insensitive, single trail… | PARTIAL | low | `internal/relay/session.go:786-788` | Effectively none. The comparator semantics agree; the only residue is that Go rejects a handful of syntactically invalid SAN strings the C comparator would merely not match, and that the se… |
| `TLS-020` | SAN iPAddress matching is textual, over 4-byte and 16-byte… | PARTIAL | low | `internal/relay/session.go:786-788` | Narrow: an upstream certificate carrying only an IP SAN is accepted by C but not matched by Go when upstream_host is a DNS name, and a certificate with an odd-length IP SAN fails the Go han… |
| `TLS-021` | Verification failure aborts the TLS handshake; the client g… | MATCH | none | `internal/server/server.go:104, internal/connection/handler.…` | — |
| `TLS-022` | The TLS handshake is bounded by the `[server] timeout` (def… | DIVERGENT | medium | `internal/connection/handler.go:202-207` | A peer that completes TCP then stalls mid-handshake pins a goroutine, an fd and one of 10000 max_connections slots for 10 minutes (indefinitely with idle_timeout: -1s) versus 30s per round… |
| `TLS-023` | `ServerHello` is queued only after the handshake completes | PARTIAL | medium | `internal/connection/handler.go:188-252, internal/connection…` | An interactive session left at a prompt for longer than idle_timeout (10m default, 30m in the shipped config) has its log connection closed by the server; the tail of the session and its Ex… |
| `TLS-024` | The relay leg performs its own TLS handshake after TCP conn… | PARTIAL | medium | `internal/relay/session.go:781-791` | Real: because the Go relay is unconditionally store-and-forward, an upstream handshake failure is never reported downstream — the client already has its log_id and commit points and reports… |
| `TLS-025` | Relay TLS settings inherit from `[server]` on a per-key bas… | ABSENT | medium | `internal/config/config.go:56-65` | An upstream sudo_logsrvd (or sudosrv-go peer) configured with tls_checkpeer = true rejects sudosrv-go's relay connection during the handshake with SSL_VERIFY_FAIL_IF_NO_PEER_CERT, and every… |
| `TLS-026` | `tls_checkhost` is not a recognised `[relay]` key, despite… | DIVERGENT | low | `internal/config/config.go:161-171` | Operator-visible only: a [relay] tls_checkhost line (or anything else pasted from sudo_logsrvd.conf) is accepted with no diagnostic and has no effect, where C refuses to start. The operator… |
| `TLS-027` | The relay does not verify the upstream certificate by defau… | INTENTIONAL | medium | `internal/relay/session.go:786, internal/config/config.go:14…` | An upstream with a self-signed or private-CA certificate that sudo_logsrvd relays to happily is refused by sudosrv-go, and with no relay CA-bundle setting (TLS-025/TLS-007) the only remedy… |
| `TLS-028` | When relay peer verification *is* enabled, the identity che… | MATCH | none | `internal/relay/session.go:786-788` | — |
| `TLS-029` | A TLS 1.3 upstream rejecting our client certificate surface… | ABSENT | low | — | A persistent upstream certificate rejection is invisible to sudo; the client reports success while transcripts pile up in relay_cache_directory. |
| `TLS-030` | `SSL_shutdown()` is attempted before the socket is closed,… | MATCH | none | `internal/connection/handler.go:180, internal/relay/session.…` | — |
| `TLS-031` | No SNI, ALPN, session-id context, renegotiation control, or… | PARTIAL | informational | `internal/server/server.go:100-103, internal/relay/session.g…` | An SNI-routing or SNI-enforcing upstream sees a server_name extension from sudosrv-go's relay that it would not see from sudo_logsrvd. No contract is broken. |
| `TLS-032` | The daemon never drops privileges, changes groups, or chroo… | MATCH | none | `cmd/sudosrv/main.go:54-73` | — |
| `TLS-033` | Startup umask is `077`, with narrowly-scoped relaxations | PARTIAL | low | `internal/config/config.go:153-154, internal/relay/session.g…` | Sudo transcripts are group-readable by default (0640/0750) where C writes 0600/0700 — a looser default posture, but with no non-daemon principal in that group in any shipped configuration. |
| `TLS-034` | I/O log files are chowned to `iolog_user`/`iolog_group` and… | ABSENT | low | `internal/config/config.go:87-95` | Operator-visible: log ownership cannot be decoupled from the daemon's own credentials, so a root-run sudosrv-go writes root-owned transcripts with no way to hand them to a sudoreplay accoun… |
| `TLS-035` | `RLIMIT_NOFILE` is raised as far as possible and there is n… | PARTIAL | low | `internal/server/server.go:214-232, internal/config/config.g…` | A client over the cap sees a TCP connect followed by an immediate close with no protocol-level explanation, where sudo_logsrvd would have served it. The fd-exhaustion consequence is refuted… |
| `TLS-036` | Wire messages are capped at 2 MB in both directions | PARTIAL | low | `internal/protocol/processor.go:20, internal/protocol/proces…` | A non-conforming client sees an abrupt EOF and reports a generic 'lost connection to log server' instead of the server's explicit 'client message too large' reason. Diagnostic quality only;… |
| `TLS-037` | Client-supplied path escapes cannot introduce path separato… | MATCH | none | `internal/storage/session.go:178-180, internal/storage/sessi…` | — |
| `TLS-038` | Every log-related open uses `O_NOFOLLOW`, and the session d… | PARTIAL | low | `internal/storage/session.go:165-171, internal/storage/sessi…` | A symlink pre-planted at $log_directory/seq would be followed and overwritten — reachable only if the operator points log_directory at a location writable by other users. |
| `TLS-039` | Configuration parsing is strict: unknown sections, unknown… | PARTIAL | low | `internal/config/config.go:161-171, internal/config/config.g…` | Operator-visible: a key pasted from sudo_logsrvd.conf, or a typo such as 'tls_min_versionn', is accepted without a word and never takes effect. Permission values are auto-corrected (config.… |
| `TLS-040` | The `-R` option randomly drops connections and is a debuggi… | ABSENT | informational | — | none for production; a testing affordance is missing (no built-in way to exercise a client's RestartMessage/resume path against this server). |
| `TLS-041` | Listener sockets set `SO_REUSEADDR`, `IPV6_V6ONLY`, non-blo… | PARTIAL | informational | `internal/server/server.go:78-110, internal/server/server.go…` | Refuted as described. The only residue is an unreachable-in-practice edge: on a host with net.ipv6.bindv6only=1, Go's favoriteAddrFamily picks AF_INET for a wildcard listen, so IPv6 clients… |

